# セキュリティポリシー — zumen

## サポート対象

**`develop` ブランチの最新コミットのみ**です。
まだ公開リリースを行っていないため、旧バージョンへのバックポートはありません。

## 脆弱性の報告方法

**公開 issue は使わないでください。**

1. **GitHub Security Advisories（推奨）** —
   [Report a vulnerability](https://github.com/meta-taro/zumen/security/advisories/new)
   から非公開でレポートを作成してください
2. それが使えない場合は、GitHub Issue に
   「セキュリティ問題のため非公開連絡を希望」とだけ書いてください。
   メンテナから Security Advisory を開きます

レポートには、再現する手順・影響範囲・確かめた環境を含めてください。
**動く攻撃コードは含めないでください。** 問題の種類と経路が分かれば足ります。

## この製品で気にしていること

zumen はローカルで動き、**外部へ送信しません。**
図の正本はファイルとして手元にあります。

| 気にしていること | いまの扱い |
|---|---|
| **ファイルの読み書き** | 殻（Tauri）では `dialog` で人が選んだ道だけを読み書きします。`fs` プラグインは登録せず、権限も与えていません |
| **依存の混入** | install script は既定で走らせません（`onlyBuiltDependencies` は空）。公開から 24 時間以内のパッケージは隔離します（`minimumReleaseAge`）。設定は [`pnpm-workspace.yaml`](pnpm-workspace.yaml) にあり、**効いているかをテストで見張っています**（`test/supply-chain.test.ts`） |
| **利用者の入力** | 図のラベルは人が書いたものとして扱い、SVG / draw.io / Mermaid のどれへ書き出すときも実体参照へ逃がします |
| **秘密情報** | このリポジトリは秘密情報を持ちません。AI エージェントは鍵・トークンを作らず・置かず・貼りません |
| **公開リポジトリへの個人情報** | 個人名・個人メールの混入を CI で検出します（`.github/workflows/oss-privacy-check.yml`）。**検査そのものが落ちた場合も失敗として扱います** |

## まだ守れていないこと

**正直に書きます。**

- **AI の API へは繋いでいません。** 繋ぐときは鍵の扱いを先に決めます
- **読み込む図を信用しています。** 手元のファイルを開く前提のため、
  悪意ある図に対する耐性は測っていません
- **リリース物の署名・配布経路はまだありません**
