# 変更の記録

**これはエージェント向けの記録です。** `zumen_about` が読んで返します。

書くのは「何を直したか」ではなく、**使う側の振る舞いが変わること**です。
内部の整理や、外から見て何も変わらない修正は書きません。
**commit の一覧が欲しいなら git を見てください。**ここはその代わりではありません。

版の付け方は次のとおりです。

- **やり方が変わった**（前の呼び方が通らなくなる）… 左の数字を上げる
- **できることが増えた**（前の呼び方はそのまま通る）… 真ん中を上げる
- **直しただけ**… 右を上げる

---

## 未リリース

### できることが増えます

- **`zumen_inspect` が `edgesUnderBoxes` を返します。**
  検証器（`edge-under-box`）にだけ入れていましたが、
  **AI が書いたあとに見るのは `inspect`** です。片方にしか無い検査は、
  半分の場面で鳴りません

- **円だけで作る動物のマークが描けます**（見本 122。キツネ・ネコ・イヌ・ゴリラ）。
  **円と円の「和」と「交わり」だけ**で、直線も自由曲線も使いません。
  **尖った耳は「2 円の交わり（レンズ形）」** —— 円を足すだけでは丸い塊にしかなりません。
  **目と鼻は、塗った輪郭の上に紙色の円で抜きます。**
  上に作図（使った円と、できあがる輪郭を太線で重ねたもの）、下に仕上がり。
  **顔の円はどれもほぼ同じで、種類を決めているのは耳の向きと鼻先の長さ**です。
  **道具は足していません**

- **囲みの名前の上に、節の名前が乗らなくなりました。**
  見本 49（アクティビティ図）で、レーンの名前「受付（レーン）」に
  開始の印の名前が乗り、**どちらも読めなく**なっていました。
  箱の外へ出す名前は箱と辺を避けていましたが、
  **囲みの名前が占めている帯**は避ける相手に入っていませんでした

- **円柱の符号が、上面の楕円に重なっていました。**
  見本 21（テーブルの関係）で、`products` `orders` `order_items` などの表名が
  **円柱の上面の楕円に串刺しにされて**いました。
  名前のほうは楕円の分だけ下げてあり、その処理のコメントは
  「**楕円に字がかかると読めない**」と言っているのに、
  **符号にだけ掛けていません**でした（見本 7 枚・15 個）

- **生け花の構成図が描けます**（見本 121。生花正風体の三主枝／池坊）。
  **長さが比で決まる図**です —— 真は花器の 2.5〜3.5 倍、副は真の 2/3、体は真の 1/3。
  **正面図だけでは奥行きが決まらない**ので、平面図と組で描きます
  （上から見ると**不等辺三角形**、手前から見ると**奥側に真**）。
  **道具は足していません**

- **時間の目盛りが、帯の下に敷かれます。**
  見本 64（施設の開館表）で、**9:00 / 10:00 … の一点鎖線が
  「一般開放」「教室」の文字を串刺しにして**いました。
  通り芯（`mark: code`）とレベル（`mark: level`）は**最前面のまま**です ——
  実物の図面でも一点鎖線は建物を貫いて見えます。
  **目盛り（`mark: tick`）は通り芯ではなく目盛り**なので、扱いを分けました
  （見本 51・57・64・71・77 が変わります）

- **トラックの積載計画図が描けます**（見本 120）。
  **軸重は、荷の重さではなく「重さ × 軸からの距離」で決まります。**
  前へ寄せすぎると前軸が超え、後ろへ寄せすぎると後軸が超えて**前輪が浮きます。**
  一般的制限値（総重量 20 t・軸重 10 t・輪荷重 5 t・高さ 3.8 m）を
  1 つでも超えたら特殊車両です。**道具は足していません**

- **`zumen_spec` の決まりが、日本語と英語で揃いました。**
  数が **ja 23 / en 19** で、**英語で読むエージェントには 4 つ足りません**でした
  （表の欄の幅、器の名前、床の同じ場所、桁揃えの空白）。
  どれも前の周で日本語側にだけ足したものです。
  **注記の並べ方の決まりも古いまま**で、`align` を入れたあとも
  「箱の幅を文字に合わせる」という**捨てたはずの回避策**を指示していました

- **消火器と屋内消火栓の配置図が描けます**（見本 119）。
  **同じ階の図に、測り方が 2 つ同時に載ります** ——
  **消火器は歩行距離 20 m 以下**（壁を回り込んだ道のり）、
  **屋内消火栓 1 号は水平距離 25 m 以下**（壁を無視した直線なので円で描く）。
  **取り違えると、届かない所ができます。道具は足していません**

- **特許図面が描けます**（見本 118。特施規 様式第 30）。
  **図面そのものに決まりがある図面**です ——
  作図領域は**横 170 mm × 縦 255 mm 以内**、**黒色で鮮明に、着色は不可**、
  実線 約 0.4 mm・引出線 約 0.2 mm、
  符号は**アラビア数字を約 5 mm 平方**で**引出線を引いて**付けます。
  **道具は足していません**

- **Camera Plot（カメラ配置図）が描けます**（見本 117）。
  **カメラをどこに置くかは、好みではなく決まりごと**です。
  話す二人を結ぶ**イマジナリーライン**を引き、
  **カメラは線の片側 180 度の半円の中だけ**に置きます。
  越えて撮ると**画面の左右が入れ替わり、視線が合わなくなります。**
  **道具は足していません**

- **引越しの搬入経路図が描けます**（見本 116）。
  置き場所を決める図ではなく、**通るかどうかを決める図**です。
  家具ごとに経路上の**いちばん狭い所**を 1 つ選んで寸法と比べます。
  **ドアは有効開口**（枠の内法 − 建具の厚み）、
  **廊下の曲がりは対角**で見ます。**道具は足していません**

- **食品工場のアレルゲン動線図が描けます**（見本 115）。
  **交差接触は、動線の設計で防ぐ** —— 現場の注意では防げません。
  人・物・空気・水を一方通行にし、物は
  **汚染 → 準清潔 → 清潔 → 準清潔 → 汚染**の順に進みます。
  **戻り道を作らない。** 戻す必要が出たら、区域の分け方のほうが間違っています。
  **道具は足していません**

- **名前の無い箱では、符号を名前の大きさで描きます。**
  符号（`tag`）は本来「名前の脇に添える小さな字」ですが、
  名前が無い箱では**符号しか書いていません** ——
  舞台照明の器具番号、花火の筒場、ダンスの踊り手、定点の番号。
  見本 **8 枚・78 個**がこの形で、**図の主役がいちばん薄い字**になっていました。
  名前があるときは、これまでどおり隅に小さく描きます

- **点字ブロックの敷設図が描けます**（見本 114。JIS T 9251:2014）。
  **線状は「進め」、点状は「止まれ」** —— 形が 2 種類しかない図面です。
  階段は先端から 30 cm 控えて縦 2 枚、幅は昇降部の有効幅員。
  **横断歩道の手前は、交差点部の両側 1 m に点状を置きません。**
  `hatch: dots` と `hatch: lines` がそのまま点状・線状に当たります。
  **道具は足していません**

- **墓石の彫刻指示図が描けます**（見本 113）。石材店が現場と工場へ出す指図で、
  **棹石の正面は家名、右側面は亡くなった人、左側面は建てた人**と決まっています。
  家紋は水鉢。人数が増えたら墓誌（霊標）へ移します。
  **存命の人の名は朱入れ**で、亡くなったら朱を抜きます。
  **道具は足していません**

- **箱の塗りに隠れて消える線を、検証器が知らせます**（`edge-under-box`）。
  `arrows: false` のとき、線は**箱より先に**描かれます
  —— 停車駅案内図の「線の上に駅の印を置く」はそれで成り立っています。
  その代わり**枠の中へ引いた線は、枠の塗りに隠れて消えます。**
  2 日で 3 回踏みました（見本 97・101・111）。どれも数の検査は 0 のままでした。
  **`arrows: true` にすれば線が上に出ます**

- **交通事故の現場見取図が描けます**（見本 112）。実況見分調書に綴じる図で、
  **寸法はすべて基点（動かない物）から測ります。**
  甲車は ①〜⑤、乙車は ア〜エ ——
  **調書の指示説明の番号と、図の番号が 1 対 1 で対応します。**
  **道具は足していません**

- **文字を寄せられます**（`nodes[].align: left | center | right`。既定は `center`）。
  注記を何行も並べたときに、**行ごとに左端がずれる**のを直すためのものです。
  zumen の文字はこれまで**すべて中央寄せ**で、行の長さが違えば左端も揃いませんでした。
  **見本 24 枚・150 行**が同じ形で出ていたので、そちらも左寄せに直しています。
  効くのは**配置図の、横組みで箱に収まった名前**だけです
  （縦組み・回した字・箱の外へ出した名前では「左」の指すものが変わるため）

- **閉塞と信号現示が描けます**（見本 111）。
  **1 つの閉塞区間に入れる列車は 1 本**で、現示は**先行列車から逆算して落ちます**
  —— 停止（赤 1）→ 注意（黄 1）→ 減速（青 1 ＋ 黄 1）→ 進行（青 1）。
  ATS-P の**速度照査パターン**を、線路と横位置を揃えて上に重ねてあります。
  **道具は足していません**

- **ダンスのフォーメーション図が描けます**（見本 110）。
  **同じ舞台をカウントごとに並べて、動きは形の差で読みます。**
  上手＝客席から見て右（舞台に立つと左）、センターを 0 として ±1 ±2 で位置を指します。
  **上手奥から下手前へのナナメの線が、客席からいちばん強く見えます。**
  **道具は足していません**

- **ヤーデージブックが描けます**（見本 109）。**面が主役**の図で、
  フェアウェイ・池・バンカー・グリーンはどれも不定形の面です
  —— 前の周で入れた `edges[].hatch` の最初の実用。
  距離は**ピンまでとエッジまでの 2 つ**を持ちます（刻む判断がそれで決まる）

- **閉じた輪の中を塗れます**（`edges[].hatch`。D31）。
  **README が長く「まだ無いもの」に挙げていた面の塗り**がこれです
  —— 池の輪郭は描けるのに塗れませんでした。
  値は節の `hatch` と同じで、**新しい語は 1 つも増えていません。**
  `close: true` の辺にだけ効き、**書かない限り何も変わりません**
  （閉じていない辺に書いたら `edge-hatch-ignored` で知らせます）

- **おせち三段重の詰め方が描けます**（見本 108）。
  品数は **3・5・7 の奇数**（偶数は「割れる」「別れる」）、
  **固形から先に詰めて隙間を残さない**（崩れず、味も移らない）。
  **道具は足していません**

- **葬儀式場の配置図が描けます**（見本 107）。**席を決めることが、焼香の順を決めること**です。
  祭壇に近い前列中央が上座、向かって右が親族席・左が一般席、
  供花は贈り主の序列で祭壇に近いほうから。**道具は足していません**

- **雑踏警備計画図が描けます**（見本 106）。見本 93（花火の保安距離）の、**人の側**です。
  **流量で判定します** —— 幅 6m の歩道橋は 720 人/分 流せて、駅から出るのは 200 人/分。
  **逆になったら手前で止める**（列車を待たせる）。**道具は足していません**

- **養蜂の巣枠配置図が描けます**（見本 105）。**どの枠に何が入っているか**の図で、
  内検のたびに書き換えるものです。**中央が育児、外が貯蜜** ——
  並べ替えると育児が冷えるので、抜いて見たら元の場所へ戻します。
  **道具は足していません**

- **牛枝肉の部分肉分割図が描けます**（見本 104）。**書く位置が体の位置**です。
  **境目は骨の位置で決まっている**ので目分量では割りません
  —— 規格で決まっているから、どこで買っても同じものが届きます。
  **道具は足していません**

- **陸上養殖の循環系統図が描けます**（見本 103）。**水を捨てずに直して戻す**図です。
  アンモニアを硝化で **NH4⁺ → NO2⁻ → NO3⁻** と弱め、**脱窒で N2 ガスにして逃がす**。
  ここまでやるから換水率 5%/日以下にできます。**道具は足していません**

- **ドローンの飛行計画図が描けます**（見本 102）。**飛ばす範囲と、人を入れない範囲**の図です。
  航空法のカテゴリー I / II / III は機体の性能ではなく
  **人が入らないようにできているか**で分かれます。**道具は足していません**

- **エージェントへの指示を 2 つ足しました**（`zumen_spec` の決まり）。
  **中に箱を入れる器には、名前を真ん中に書かない**こと、
  **物どうしを床の同じ場所に置かない**こと。
  どちらも今日、見本で実際に起きた間違いです

- **野外ステージの PA 配置図が描けます**（見本 101）。**距離が時間になる図**です。
  30m 後ろのスピーカーはメインより **88 ms 早く届く**ので遅らせます
  （遅延 ＝ 距離差 ÷ 音速 × 1000、音速は c ＝ 331.5 ＋ 0.6 × 気温）。
  計算値に **+5〜15 ms** 足すと、音がメインの方向から来ているように聞こえます。
  **道具は足していません**

- **裁ち合わせ図が描けます**（見本 100）。**布の上のどこに型紙を置くか**だけの図です。
  **型紙は回しません** —— 地の目線は耳と平行でなければならず、
  回すと布の伸び方が変わって着たときに歪みます。**道具は足していません**

- **法廷の配置図が描けます**（見本 99）。**席がそのまま役割**の図です。
  「右陪席」「左陪席」は**裁判長から見た向き**なので、傍聴席から見た図では
  左右が入れ替わります。検察官席と弁護人席の左右は**法律で決まっていません**。
  **道具は足していません**

- **手直しが壊れないことを、見本ぜんたいで測るようにしました**
  （`test/gallery-pins.test.ts`）。人が 1 か所だけ位置を直したあと、
  AI が手直しを知らない提案を出してくる —— という往復を全見本で回します。
  **99 枚すべてで手直しが残り、競合は 0 件**でした

- **パレット積付図が描けます**（見本 98）。T11（1,100 × 1,100）に
  **4 つの積み方を並べて比べます** —— ブロック・交互列・れんが・風車。
  **段ごとに向きを変えると横の力に強くなる**こと、
  **風車はいちばん強いが真ん中が空く**ことが、絵と表の両方で読めます。
  **道具は足していません**

- **防犯カメラの視野図が描けます**（見本 97）。**扇形が図の中身**です。
  DORI（IEC/EN 62676-4）で「**写っている**」と「**誰か分かる**」を分け、
  カメラごとに識別 250 / 認識 125 / 観察 62 px/m の距離を出しています。
  **道具は足していません** —— 扇形は
  **自分自身への辺に `via` を並べて `close: true`** で描けました

- **オーケストラの配置図が描けます**（見本 96）。対向配置と現代配置を並べて、
  **どこが動くかを表で言います。** 動くのは第 2 Vn と Vc と Cb の側だけです。
  **道具は足していません**

- **はみ出して重なっている箱を知らせます**（`zumen_inspect` の `straddles`）。
  `overlaps` は**入れ子も数える**ので配置図では鳴りっぱなしで、誰も見ていませんでした。
  **直すところがあるのは、どちらも相手を含んでいない重なり**です

- **避難所レイアウトが描けます**（見本 95）。**面積と間隔が図の中身**です。
  スフィア基準（1 人 3.5m² 以上、トイレは 20 人に 1 基・男女比 1:3、
  水は 1 人 1 日 15L）と、発熱者の別動線。**道具は足していません**

- **ヘアカラーのブロッキング図が描けます**（見本 94）。**塗る順が図の中身**です
  —— 染まりにくいネープから塗り、体温で早く進む顔まわりを最後に塗って、
  全部を同じ仕上がりに揃えます。根元と既染部で薬も時間も違います。
  **道具は足していません**

- **花火大会の保安距離図が描けます**（見本 93）。**円が図の中身**で、飾りではありません。
  玉の号数ごとの保安距離（火薬類取締法施行規則に基づく県の技術基準）、
  筒場・観覧席・立入禁止区域・風向計。**道具は足していません**

- **防虫モニタリングの定点配置図が描けます**（見本 92）。
  食品工場のゾーニングと、ライト／粘着／フェロモンの定点、捕獲数の表。
  **定点は動かさない** —— 番号と位置が固定されているから、月どうしを比べられます。
  **道具は足していません**

- **入らない符号を、黙って落としません**（`zumen_inspect` の `hiddenTags`、
  `validate` の `tag-hidden`）。印が小さいと符号は描かれません。
  **落とすこと自体は正しい**（伏図の小梁は幅 20px しかありません）。
  **黙って落とすのが問題**でした ——
  入れて回したところ、**見本 3 枚で符号が消えていました**（26・27・92）

- **リンゴ高密植栽培の樹形図が描けます**（見本 91）。トールスピンドル。
  樹高 3.2m／樹間 0.9m／列間 3.0m を**縮尺つき**で、側枝の下垂誘引と
  切り返し・間引きの区別まで。**道具は足していません**

- **`validate` が、置いたあとの形も見ます**（配置図だけ。`text-overlap`）。
  **文字どうしの重なりは、正本だけを読んでも分かりません。**
  これまでは `zumen_inspect` を叩けるエージェントにしか見えておらず、
  **人が CLI で確かめられませんでした。**`pnpm validate` で出ます

- **二重線が引けます**（`edges[].line: double`）。相続関係説明図・家系図は
  **婚姻が二重線、親子が単線**と決まっていて（法務局の記載例）、
  **太さでも破線でも代わりになりません。**足したのは**線の形の名前**であって、
  意味の語（`marriage`）ではありません（D27）

- **相続関係説明図が描けます**（見本 90）。不動産の相続登記に添える書面です。
  （相続）（分割）（相続放棄）（被代襲者）（代襲者）の付記まで入れました。
  **相続放棄した人も省きません** —— 省くと相続人の範囲が戸籍と合わなくなります

- **コンテナ船の積付図（Bay Plan）が描けます**（見本 89）。**線が 1 本も無い図**で、
  セルの位置がそのままコンテナの住所（ISO 9711-1 の Bay-Row-Tier）になります。
  揚地の色分け・リーファー・IMDG の印つき。**道具は足していません**

- **UI の画面遷移とレスポンシブ構造が描けます**（見本 85〜88）。
  Checkout の 5 段（エラーの戻り先つき）、CRUD 管理画面（削除は確認を挟む）、
  モバイルアプリの Bottom Navigation、レスポンシブ LP。
  レスポンシブは**縮小ではなく、節が消えたり増えたりすること**なので、
  絵の横に**差分の表**を置いています。**道具は足していません**

- **文字どうしの重なりを知らせます**（`zumen_inspect` の `overlappingText`。配置図だけ）。
  `overlaps` は**箱**を数えるので、枠の中に節を入れる図では鳴りっぱなしになり、
  誰も見なくなっていました。**箱の重なりは意図のことがあるが、
  文字の重なりはほぼ必ず間違い**なので分けて数えます。
  入れて回したところ、**見本 3 枚で実際に重なっていました**（43・74・86）

- **路線の色が使えます**（`palette` ＋ `nodes[].color` / `edges[].color`）。
  **色は正本が持ちます** —— zumen は銀座線がオレンジであることを覚えません。
  色が記法そのものである業界（路線図・配管の識別色・電線の相色）のための、
  `DESIGN.md` §7 の例外です。**色だけに頼らせない**ため、
  薄すぎる色（`color-faint`）と、符号に路線記号が出ていないもの
  （`color-without-code`）を知らせます（仕様 §3.0.13）

- **中古車の査定図が描けます**（見本 84）。**書く位置が車体の位置**で、
  JAAI の減点記号（A / E / U / W / S / C / Y / X / XX）を使います。**道具は足していません**

- **東京の地下鉄 13 路線（メトロ 9 ＋ 都営 4）が描けます**（見本 81）。
  55 駅。**駅名の置き場所は、検査に教わりながら 4 回で 0 件**にしました

- **駅の配線略図が描けます**（見本 83）。**線が主役**の図で、
  上下の並びは**番線**であって高さではありません。**道具は足していません**

- **塗り潰した印の「外」に出した名前が、読めるようになりました。**
  塗りの上の文字を地の色へ反転する処理が、**外へ出した名前にも効いていて、
  白い紙に白い字**で描かれていました

- **階が正本の言葉になりました**（`floors` ＋ `nodes[].floor` ＋ `edges[].vertical`）。
  **階の枠と階名は機械が描きます** —— 箱を足しても枠がずれません。
  **箱は 1 px も動きません**（`floor` は「どの枠に属するか」を言うだけ）。
  縦動線は `stair` / `escalator` / `elevator`（**JIS Z 8210** の語）で、
  **同じ階どうしを結ぶと知らせます**（仕様 §3.0.16）。
  これは 3D の下ごしらえでもあります —— 3D は**同じ正本を高さ方向に積む描き手**（D26）

- ~~**階層のある乗換案内が描けます**（見本 82）。~~
  1F / B1 / B2 を並べ、**同じ x に置いたものは上下で同じ場所**という約束にすると、
  階段・エスカレーター・エレベーターが「どこで上下するか」を指せます。
  **道具は足していません**

- **中央線快速の停車駅一覧と、東京メトロ 9 路線の路線図が描けます**（見本 80・81）。
  中央線は**快速が土休日に高円寺・阿佐ケ谷・西荻窪を通過する**ところが帯の欠けで読めます。
  メトロは 9 路線 42 駅。**道具は足していません**

- **山手線の路線図と、梅田の乗換関係図が描けます**（見本 78・79）。
  山手線は**環状なので輪を閉じて**描いています（`close`）。30 駅・JY ナンバリング・
  主要駅の乗換つき。梅田は**同じ地名に 7 駅**あるという、路線図では 1 点に見える所を
  ほどいた図です。**道具は足していません**

- **舞台照明仕込図が描けます**（見本 77）。
  実物は **Light Plot（平面）＋ Channel Hookup（一覧表）の 2 点セット**で運用されるので、
  同じ形にしました。1 灯あたり Ch・器具・位置・回路・色・フォーカスの 6 項目は、
  **図だけでは載りません**

- **紙の外へ出て切れた名前も、`crowdedNames` が知らせます。**
  重なっていなくても、**切れている文字は読めません**

- **UI の構造図が描けます**（見本 74・75・76）。
  **完成デザインではありません** —— 色も書体も装飾も持たず、
  「何が・どこに・どの順で・PC と SP でどう変わるか」だけを持ちます。
  **レスポンシブの差分表**と、**ワイヤーフレームつきの画面遷移図**が主役です。
  **道具は足していません**

- **塗り潰した箱の符号が読めるようになりました。**
  本文は地の色へ反転していたのに、**符号（`tag`）だけ元の色のまま沈んでいました**

- **歯周精密検査チャートとサビキ仕掛け図が描けます**（見本 72・73）。
  どちらも**専用ソフトか紙の様式**で作られているもので、
  汎用の作図ソフトが得意な分野ではありません。**道具は足していません**

- **列車運行図表（ダイヤグラム）が描けます**（見本 71）。
  **駅の間隔を営業キロに比例**させてあるので、**線の傾きがそのまま速度**になり、
  待避（追い抜き）とすれ違いが目で読めます。**道具は足していません**

- **車両の編成図が実物の形になりました**（見本 29）。
  2 段に折り返して**連結が逆走して見えていた**のを 1 列にし、
  編成の向き・パンタグラフ・設備記号を足しました

- **リバーシの局面図が描けます**（見本 70）。
  「オセロ」は登録商標なので、一般名の「リバーシ」で描いています

- **仕様の `nodes` / `edges` の表が、実装に追いつきました。**
  `marker` も `hatch` も `write` も `via` も `curve` も載っておらず、
  表だけを読むと「自由なキー」に見えていました

- **囲碁・将棋・チェスの盤とビンゴカードが描けます**（見本 66〜69）。
  **道具は足していません。** 碁盤は「線の交点に石」、将棋とチェスは「マスに駒」、
  ビンゴは表 —— 同じ盤でも中身が違うので、描き方も違います

- **`nodes` に書いた `appearance` を知らせます**（`appearance-in-nodes`）。
  体裁は人が `pins` に書くもので、**`nodes` に書いても効きません。**
  これまでは黙って無視していたので、**書いた人は効いていると思ったまま**でした

- **フェリーの発着表・施設の開館表・ビンゴ大会の進行表が描けます**
  （見本 63・64・65）。**道具は足していません。**
  フェリーは車両航送の可否と運航日が便ごとに違うところ、開館表は
  「帯を描かないことが休館の意味」、進行表は担当と会場配置の言葉を揃えるところが要点です

- **書き出した SVG に、図の題が入ります**（`<title>`）。
  絵の見た目は変わりません（`<title>` は表示されません）が、
  **読み上げと貼り先の説明に届きます** —— SVG を 1 枚だけ人へ渡す使い方のためです

- **輪を閉じられます**（`edges[].close: true`）。池・陸上競技場のトラック・
  ロータリー・車両限界・車の外形 —— **輪郭が閉じていることが「面」の意味そのもの**で、
  1 区間でも開くと面に見えません。継ぎ目も滑らかで、**閉じた輪に矢印は付きません**
  （見本 62・仕様 §3.0.15）。**面の塗りはまだありません**

- **線を曲げられます**（`edges[].via` ＋ `edges[].curve: smooth`）。
  `via` に通る点を書くと、その点を**通る**曲線になります（近くを通るだけにしません）。
  道路の平面線形・河川・庭園の園路・配管の曲がり —— **曲がっていること自体が内容**で、
  直角では代用できない図のためです（見本 61・仕様 §3.0.15）。
  **人が `pins.waypoints` で曲げれば、そちらが勝ちます**（`at` と `pins.position` と同じ）

- **商店街の店舗案内図・フードコートの配置図・ドームの座席等級図が描けます**
  （見本 58・59・60）。**道具は足していません。**
  業種と席種は「色」ではなく**模様**（`hatch`）で示します —— 白黒でも、
  色覚特性でも、縮小しても失われません（`DESIGN.md` §7）。
  ドームの外周が曲線でないのは、まだ曲線を引けないためです（課題 2）

- **学校の時間割・バスの時刻表・登山のコースタイム図が描けます**（見本 55・56・57）。
  **道具は足していません** —— 3 枚とも今ある道具だけで描けました。
  表はセルを隙間なく並べると罫線が繋がります。
  登山の縦断図は、横を累積時間・縦を標高にした `mark: tick` の両軸で、
  **線の傾きがそのまま登りのきつさ**になります

- **駅名が縦に組めます**（`write: down`）。字を 1 つずつ上から積みます
  —— **回す（`along`）のとは別**で、実物の路線図は寝かせていません。
  副題は右の列へ小さく（ふりがなの定位置）、**ラテン文字だけは寝ます**
  （JIS X 4051 の横倒し。仕様 §3.0.14）

- **1 路線の のりかえ案内図が描けます**（見本 54）。
  帯・駅の刻み・駅ナンバリング・のりかえの記号を 1 枚に並べたもの。
  大阪メトロがホームと車内に貼っている、あの形です

- **停車駅案内図と駅の時刻表が描けます。**
  ホームに貼ってある、どの種別がどこに停まるかの表と、時／分の表です
  （見本 52・53）。**帯が種別、白丸が停車**で、実物と同じ読み方です。
  **ネットにこの形で置かれていない情報**なので、
  正本をテキストで持てることに意味があります

- **名前は、線を避けて置かれます。**
  上下左右に加えて**斜め 4 方向**も試すようになりました。
  上下左右へ路線が抜ける乗換駅は 4 方向とも塞がっていて、
  **駅名の上を路線が走っていました**（実物の路線図も、そこは斜めに書いてあります）

### 直りました

- **丸い印の塗りが、丸になりました。** `marker: circle` に `hatch: solid` を
  付けると、**丸の上に四角が乗っていました**（模様も印の形で切り抜きます）

- **印の中の符号が、印の真ん中に来ます。** 左上に寄せていたので、
  二重丸（乗換駅）の**内側の丸が駅番号を横切っていました**

- **`label: ""` は、文字を出しません。** 名前を持たない印（停車駅案内図の ●）で
  **id がそのまま図に出ていました**

- **外へ出した名前が、紙からはみ出さなくなりました**（右と下へ紙を広げます）

- **「投影すると字が読めるか」が、その図に実際に出る字で測られます**
  （`inspect` の `smallestText`）。11px の決め打ちだったので、
  **10px の字（符号・寸法・方位）がある図を 1 割ぶん甘く見ていました。**
  見本で下回る図は 12 枚ではなく **18 枚**でした

- **駅の時刻表（見本 53）が、罫線の繋がった表になりました。**
  セルを 4px 空けていたので、実物のような表ではなく札の並びに見えていました

- **小さい箱に、壁の厚みが効かなくなりました。**
  縮尺のある平面図に凡例の見本（26×20）を置くと、
  **壁でほとんど枠になっていました。** 壁が短辺の 1/5 を超える箱は線で描きます

- **縦の目盛り（`grid.y` の `mark: tick`）の名前を、線が横切らなくなりました。**
  線を 2 本引いていて、長いほうが名前の上を通っていました

- **薄い色を、ライトとダークの両方の地で見るようになりました**（`color-faint`）。
  白地で足りていても**暗い地で沈む色**があり、ダークの図では線が見えていませんでした

- **日本式の路線図が描けます**（`weight: thick`）。太い線は**角と端が丸まります。**
  45 度は座標で、駅番号は `tag`、乗換駅は `marker: double`（仕様 §3.0.12）

- **工程表・ガントチャートが描けます**（`mark: tick`）。
  時間軸は**名前だけを目盛りの上に**書き、**寸法を引きません**
  —— 名前が既に時刻を言っているので、引くと意味のない数字が並びます（仕様 §3.0.2）

- **電子回路図が描けます**（`symbol: resistor` など 13 記号・IEC／JIS の形）。
  **枠を描かず記号だけ**が出て、部品名と値は脇に出ます。
  **記号は足の位置を自分で知っていて、配線は自動で直角に曲がります**（仕様 §3.0.10）。
  トランジスタとオペアンプは足に名前が要るので、まだ描けません

- **UML のユースケース図・アクティビティ図が描けます。**
  印に `ellipse`（ユースケース）`diamond`（判断）`bar`（フォーク／ジョイン）を足しました
- **`ends: { to: none }` で、その辺の矢印を止められます。**
  UML の関連線は無向なので、書いたのに矢印が出ると別の意味になります。
  **`ends` を書かないことと、`none` と書くことは違います**（仕様 §3.0.8）

- **UML のクラス図・状態遷移図が描けます。** 端の記号に
  `triangle`（汎化・実現）`diamond`（集約）`solid-diamond`（コンポジション）を、
  辺に `line: dashed`（実現・依存）を足しました。
  **汎化と実現は線種でしか区別できません**（仕様 §3.0.9）。
  **属性と操作は描きません** —— コードに書いてあるものを図へ写すと、
  コードが変わった瞬間に図が嘘になります

- **辺の端に記号を付けられます**（`ends: { from: bar, to: crow }`）。
  **ER 図の多重度は、文字ではなく記号で書きます** ——
  データベースをやる人は端の形で読み、文字だと**辺が増えるほど消えます。**
  棒＝1／鳥の足＝多／丸＋鳥の足＝0 以上（仕様 §3.0.8）

- **材料と区域を模様で描き分けられます**（`hatch: solid` / `dots` / `lines` / `cross`）。
  **模様は飾りではありません** —— 図面を縮小すると**文字は消えますが模様は残ります。**
  アスコンは塗り潰し、砕石は点、地盤は斜線、立入禁止は格子。
  文字のところで模様を切ります（仕様 §3.0.7）
- **丸い印に入る短い文字は、中に書きます。** 盛付指示書の丸数字や駅番号がそれで、
  **中に書くことがその印の意味**です

- **配置図で、丸い印を打てます**（`marker: circle` / `double` / `none`）。
  路線図の駅・経穴・計器。**`type` は増えていません** ——
  `type` は「これは円柱だ」＝物の種類、`marker` は「ここは丸で印を付ける」＝
  図の描き方で、**4 つで閉じます**（仕様 §3.0.6）。
  名前は印の外へ、符号は印の中に出ます

- **断面図・立面図が描けます。** `grid` の横の基準線に `mark: level` を書くと、
  丸の符号ではなく**三角の高さ記号と値**（`GL±0` / `2FL+3,200`）で出ます。
  **`kind` は増えていません** —— 測り方は配置図と同じ（`y` を高さとして読む）で、
  違ったのは記号だけでした（仕様 §3.0.2）

- **`nodes[].radius` で、届く範囲を円で描けます。** クレーンの作業半径・
  消火器の警戒区域・影の離隔。**これが無いと総合仮設計画図が成り立ちません**
  （[#4](https://github.com/meta-taro/zumen/issues/4) の原題がそれです）。
  `scale` があれば `R=25,000` と書き添えます。破線で描きます ——
  実線にすると、**届く範囲と物の輪郭が見分けられなくなる**ためです（仕様 §3.0.5）

- **`arrows: false` で、向きの無い線を引けます。** 路線図や単線結線図の線は
  「繋がっている」ことだけを言っていて、向きがありません。
  向きの無い線は**箱の下に敷きます**（上に描くと駅の印を串刺しにします）
- **箱に入らない文字を、落とさずに外へ出します。** 以前は落としていたため、
  路線図で**駅名が 1 つ残らず消えました**。実物の図面も、狭い部屋の名前は箱の外に書きます

- **壁に厚みが付きます**（`wall: { mm: 105, outer: 180 }`）。
  **1 本線では壁に見えません** —— 部屋を分ける境界線には見えても、壁には見えません。
  1/100 前後の図では、実物も塗り潰し（ポシェ）で描かれます。
  `scale` が無ければ何も変えません（仕様 §3.0.3）

- **通り芯・寸法線・方位記号を描けます**（`grid` / `scale` / `north`）。
  オーナーから「工事現場では使えない。**不動産の間取り図レベル**」と指摘され、
  そのとおりでした。**寸法が無い図では、何も建てられません。**
  一点鎖線が建物を貫いて外まで伸び、両端に符号を丸で囲んで置きます。
  寸法は「芯どうし」「総寸法」の 2 段（仕様 §3.0.2）。
  **`scale` が無ければ数値を出しません** —— 知らない縮尺で数値を書くより、
  出さないほうがましだからです

- **`wrap: true` で、長い鎖を折り返せます。** 7 工程の業務フローが
  横 2584 × 縦 124（比 20.8）で出ていました。**既定は折り返しません**
  —— 折り返すと辺が右から左へ戻り、[#1](https://github.com/meta-taro/zumen/issues/1)
  で報告された壊れ方と同じ絵になるためです（仕様 §3.0.1）

- **`nodes[].tag` で符号を書けます。** `C1`（柱）`G1`（大梁）`2"-CS-101`（配管）
  `LBS-1`（電気）。箱の左上に小さく出ます。**`type` は 11 語のままです**
  —— 業界の専門性は形ではなく符号で表されているからです（仕様 §3.1.3）
- **`nodes[].openings` で建具（扉・窓）を描けます。** `door` / `double` / `slide` /
  `window` / `open` の 5 種。配置図でだけ効きます（仕様 §3.1.2）

### 見え方が変わります

- **配置図の文字の入れ方を、4 段で選ぶようになりました。**
  そのまま中に入る → 背が低いだけなら**名前と副題を 1 行に繋いで**中へ →
  横に入らず縦になら入るなら帯に沿って縦へ → どれも駄目なら図の縁の外へ。
  以前は「高さ 34px 未満は外」としていたため、
  **映画館の横通路（高さ 12px）の名前が、隣のブロックの上に落ちていました**
- **名前は入るが副題が入らないときは、副題だけ横に回して添えます。**
  駐車場の区画で `W1` まで縦書きになっていました。
  実物の区画割図も、幅の数値は区画に沿って縦に書いてあります
- **符号がある箱では、中の文字を符号の分だけ下げます。**
  車両編成図で「2 号車」と「モハ 100-1」が重なっていました

- **細い帯の名前を、帯に沿って縦に書きます。** 用水路（幅 4m）や廊下は、
  名前が横には入らないが縦には入ります。外へ出すと隣の帯の名前と団子になりました
- **箱に入らない文字は、図の縁に近いほうへ出します。** 下へ固定していたため、
  ほ場整備の図で**幹線農道の名前が田んぼの中に落ちました**

- **建築設備の系統図に、実際の値が入りました。** 電気は回路番号・ブレーカ容量・
  電線サイズ（`回路 3` / `200V 30A・VVF2.6`）、給排水は機器番号と呼び径
  （`WH-1` / `給湯 13A` / `排水 75A`）。
  **箱と矢印だけでは、現場では使えません**

- **配置図で、外周の壁が部屋に沿います。** 囲みと中身の間を 24px 空けていたため、
  建物の周りに廊下があるように見えていました

- **配置図が、平面図として描かれます。** 実物の間取り図と並べたところ別物でした。
  **角丸をやめて四角に**（隣の部屋と壁を共有して見えるように）、
  **部屋どうしの矢印を描かない**、**外周を太い実線に**（破線の囲みをやめる）、
  **部屋名と広さを小さく**しました
- **配置図で、入らない文字を出しません。** 伏図の小梁（幅 20px）に
  「小梁 300×600」が `0×60` と切れて、隣の部材へはみ出していました
- **配置図では、大きいものから先に描きます。** スラブが大梁と柱を塗り潰していました

### 直しました

- **図が、地の色を自分で持つようになりました**（D33）。
  暗色の図（`-dark.svg`）を**そのまま開くと、箱の外に書いた注記が 1 行も
  見えませんでした** —— 文字は明るい灰で正しいのに、白い紙の上では白い字になります。
  紹介ページは暗い背景の上に置いているので、そこでは正しく見えていました

- **「描いてあるのに、画用紙の外にある」ものを、まとめて直しました。**
  数の検査はどれも 0 のままで、**紙の外にあることを誰も見ていません**でした。
  見つかったのは 6 種類です。

  | 何が | どこで |
  |---|---|
  | 辺の通り道 | 見本 07 —— 差し戻しの線が **52 px 手前で切れて**いた |
  | 範囲の円 | 見本 30 —— 「クレーンの作業半径つき」なのに**円が切れて**いた |
  | レベルの名前 | 見本 42・43 —— 断面図の「GL±0」が**矢印の先だけ**残っていた |
  | 通り芯の符号 | 見本 27・28・31 —— **上の X1〜X6 が 1 つも描かれていなかった** |
  | 通り芯の位置 | 見本 71 —— 右端の時刻「9:00」が紙の **43 px 外**にあった |
  | 囲みの名前 | 見本 39 —— 経絡の名前が紙の **110 px 外**まで伸びていた |

  **見本ぜんぶを毎回数える試験**（`test/paper.test.ts`）を足しました。
  個別に直すだけでは、次に別の形で戻ってきます

- **断面図のレベル名が、画用紙の左で切れていました。**
  見本 42（擁壁）・43（舗装構成）で「天端 +3,000」「GL±0」「底版下 -350」が
  **50 px ほど切れ、矢印の先だけが画用紙の縁に残って**いました。
  余白の見積もりに**矢印の位置が入っていなかった**ためです。
  **断面図でレベルが読めないなら、断面図ではありません**

- **範囲の円（`radius`）も、画用紙の中に入ります。**
  見本 30 の紹介文は「**クレーンの作業半径つき**」なのに、
  **その円が画用紙の左と上で切れて**いました。下の直しと同じ穴です

- **画用紙の大きさを、辺の通り道も見て決めます。**
  四隅を測っていたのは**箱だけ**でした。そのため
  **箱より下や右へ回り込んだ線が、画用紙の外で切れて**いました。
  見本 07（稟議の流れ）では、役員会と経理から出た線が
  **52 px 手前で切れて、どこへ行くのか読めない絵**になっていました。
  見本 13（学校の年間）も同じです。17 枚の図が少し大きくなります。
  `via` だけで描く形（池・グリーン・体の輪郭）も、
  **囲む箱を置かなくても切れなくなりました**

- **菱形の印の符号が、斜めの辺に食われていました。**
  符号は角に置く決まりでしたが、菱形の角には面がありません。
  丸と同じく**真ん中へ置く**ようにしました（見本 92 の `ST-2` など）

- **別の箱の模様の上に載った文字が、埋もれていました。**
  仕様は「模様の上に文字を重ねると読めないので**文字の下地を抜く**」と書いてありますが、
  抜いていたのは**自分の箱が持つ模様だけ**でした。
  Bottom Navigation の帯（塗り潰し）の上のタブ名が、**同じ濃さで沈んでいました**（見本 87）。
  文字が丸ごと入っている箱に模様があれば、下地を抜きます

- **見ないと分からない崩れが 2 種類ありました**（数の検査はすべて 0 のまま）。
  **表の欄から値が飛び出して行が空に見える**（3 枚）のと、
  **箇条書きの行頭が階段状にずれる**（18 枚・106 行）。
  前者は `zumen_inspect` の `adriftNames` と `validate` の `name-adrift` で
  知らせるようにし、後者は**箱の幅を文字の幅に合わせて**直しました
  （文字は箱の中央に置かれるので、幅がばらばらだと行頭が揃いません）

- **閉じた輪の始まりで、曲線が跳ねていました。**
  自分自身への辺で閉じた形を描くと、**出口と入口が同じ節の縁で 1〜2 px 離れて並び**、
  そこから制御点が跳ねて **8 px ほどの角**が出ていました。
  ほとんど同じ点は 1 つに畳みます（**人が書いた点は畳みません** ——
  畳むのは 2.5 px 未満の隣り合わせだけ）。**実物をブラウザで見て見つけました**

- **文書が実物と食い違っていました**（棚卸し）。
  日本語版 README は**まだ「23 枚」**（実際は 107 枚）、
  「未解決：ありません（D1〜D12 が確定）」（実際は D30 まであり、未決は D3）、
  `pnpm gui:check` の項目数も古いまま。PRD §6 は
  **「これが決まるまで実装に入れない」**と書いたままで、その D2 は既に決まっています。
  ぜんぶ直し、**日本語版の枚数もテストで止める**ようにしました

- **MCP サーバが、一度も動かされていませんでした。**
  `src/mcp.ts` はどのテストからも読まれておらず、**カバレッジの表にすら載らず**、
  `pnpm coverage` が 98.6% と言っている横で
  **エージェントが実際に叩く口だけが無検査**でした。
  サーバを立てて話す検査を入れ（道具の一覧・説明・呼び出し・開けていない口）、
  そのとき **`zumen_list` と `zumen_read` の説明が 1 行しかない**ことも見つけたので、
  **何を先にすべきか**（既にある図を読んでから直す）を書き足しました

- **仕様に載っていない検査が 58 件中 22 件ありました。**
  仕様は**別の実装が同じファイルを読み書きできる**ように書いてある文書なので、
  抜けているとその実装は同じ指摘を出せません。ぜんぶ書き、
  書き忘れをテストで止めるようにしました

- **構成図の線が、節を増やすと絡んでいました。** 見本の構成図はどれも 7〜14 節で
  交差 0 でしたが、**実務規模で試したことがありませんでした。**
  組み立ての手間（`elk.layered.thoroughness`）を上げて、
  **40 節で 33 → 22、60 節で 88 → 51** に減らしました（図も小さくなります）。
  代償は組み立ての時間（80 節で 36ms → 77ms）。
  **ただしこれで実務規模が読めるようになるわけではありません** ——
  30 節を超えると印刷でも字が読めず、図を分けるしかありません

- **足した観測値を、道具の説明に書き忘れていました**
  （`straddles` と `hiddenTags`）。**説明に無い観測値は、
  エージェントにとって存在しないのと同じ**です。
  書き忘れをテストで止めるようにしました

- **README に書いた数が、実物とずれていました**（検査の件数が 57 と書いてあって、
  実際は 58）。見本の枚数は一晩で 3 回手で直しました。
  **数だけは機械に数えさせる**ようにしました（`test/docs-numbers.test.ts`）

- **自分自身への辺に `via` が無いと、長さ 0 の線に矢印だけが出ていました**
  （節の真ん中に黒い粒）。**知らせるものがありませんでした。**
  `edge-self-open` で知らせます。あわせて
  **「自分自身への辺 ＋ `via` ＋ `close: true` で閉じた形が描ける」**ことを、
  `zumen_spec` の見本と仕様に書きました —— **書いていない書き方は、
  エージェントにとって存在しないのと同じ**です

- **見本 10 枚に、物どうしが同じ場所を取っている間違いが埋まっていました**
  （上の `straddles` を入れて見つけました）。
  **冷蔵ケースと弁当什器が床の同じ場所**（見本 15）、
  **クレーンが車輌通路を塞いでいる**（見本 30）、
  **消防車が立入禁止区域へはみ出している**（見本 93）、
  終点の行き先が 2 つ重なっている（見本 81）ほか、
  見出し・注記・定点が他のものに乗っていたもの（91・92・94・95）。
  わざと重ねている図（伏図の柱、断面の水抜管、盤の上の石、舞台のバトン）は
  **理由を書いて外してあります**

- **「投影には小さすぎます」が、見本 95 枚のうち 36 枚（38%）で鳴っていました。**
  路線図・査定図・仕込図・積付図 —— **分けようのない図ばかり**で、
  毎回そのまま流されていました。**鳴りっぱなしの指摘は読まれなくなります。**
  ところがその 36 枚は**全部 A3 の下限は通っています。**
  `zumen_inspect` に `tooSmallToPrint` を足し、
  **「これは印刷して読む図で、投影に向かないだけ」**と言えるようにしました。
  **両方が真のときだけ、本当に直すところがあります**
  （下限は JIS Z 8313 の最小文字高 2.5mm ÷ A3 短辺 297mm ＝ 0.84%）

- **紹介ページの見本一覧を、分類して並べ直しました**（人が見て出た指摘）。
  それまでは**足した順**に近く、路線図が 3 か所に散っていました
  （25 / 45 / 52〜54 / 71 / 78〜83）。**探すときは「鉄道の図」を探すのであって、
  「78 番」を探すのではありません。**9 つに分け、区切りの見出しと件数を出し、
  絞り込みを上に貼り付くようにしました
- **見本一覧は、手で書くのをやめました**（`scripts/gallery.mjs` が組み立てます）。
  見本を足してページに足し忘れる事故が毎回起きていました。
  **絵の大きさも SVG から書く**ので、遅延読み込みでも下の図が飛びません
  （`pnpm gallery:check` が、ページの古さと**分類に入れ忘れた見本**を落とします）

- **土木の図が、ミリで書かれていました。** ダムの平面図に `200000`、
  圃場整備に `125000`、保安距離の円に `R=200,000`。
  **桁を数えないと読めない数字**は、図面としては書いていないのと同じです。
  **単位は縮尺が決めます** —— 1 px が 100 mm 以上（1:100 より小さい縮尺）の図は
  **m**（`45 m`）、それより大きい縮尺は**これまでどおり mm**（`7,000`）。
  **数値の大きさでは決めません**（同じ図に `45 m` と `5,000` が並びます）。
  あわせて、ミリの数値も 3 桁ごとに区切るようにしました（`3600` → `3,600`）

- **`zumen_spec` の見本に、足した語が載っていませんでした。**
  `line: double`（D27）も、`marker` の `ellipse` / `diamond` / `bar` も、
  **一度も書かれていませんでした。**`zumen_spec` はエージェントが最初に読むもので、
  **そこに無い語は、エージェントにとって存在しないのと同じ**です。
  語彙の一覧と見本が食い違わないことを、テストで見張るようにしました

- **`color-without-code` が、鳴るべきところで鳴っていませんでした。**
  節ごとに「`tag` が鍵で始まっているか」を見ていたので、
  **`tag` の無い節では一度も鳴らず**（符号がどこにも無いのがいちばん危ないのに）、
  **`tag` が別の意味を持つ図では誤って鳴って**いました（積付図の `tag` は
  リーファーと危険物の印で、揚地の符号ではありません）。
  **図ぜんたいで「その鍵が文字として出ているか」だけを見る**ようにしました。
  実物の路線図も、駅ごとに色名は書きません —— **凡例に 1 回書いてあります**

- **印の中に、符号と名前の両方を入れていました。**
  路線図で**駅名が駅番号に重なって**いました。
  印の中に置けるのは 1 つだけで、**符号があるなら中はそれのもの**です
- **丸い印の外へ出す文字が、丸の線に触れていました。** 5px 離します

- **負の座標に置いたものが、画用紙の外へ落ちて消えていました。**
  右下の端だけを測っていたためです。工程表は**本体より左に見出しの列**を置くので、
  `at: { x: -120 }` はふつうの書き方です（仕様 §3.0.11）。
  **負が無ければ 1 px も動かしません**

- **壁の厚みが、丸い印にも効いていました。** 座席図で、壁厚 150mm（6px）が
  21px の座席の丸を塗り潰していました。**壁の厚みは「部屋」のもの**です
- **枠なし（`marker: none`）の文字が、箱の外へ出ていました。**
  枠が無いのに「枠の外」へ出しても意味がありません（座席図の列名 A〜F）

- **外へ出した名前が、他の箱に乗らなくなりました。** 上下を試して、
  他の箱にも先に置いた文字にも当たらないほうを選びます。
  どこも空いていなければ **`zumen_inspect` の `crowdedNames`** で知らせます
  —— **消しはしません**（部屋の名前が消えるのは、重なるより悪いため）。
  この検査で、**ダムの図と映画館の図の不具合が見つかりました**

- **文書に載せていた見本が、zumen 自身の検証を通りませんでした。**
  紹介のページ・README・仕様・MCP の雛形の 4 か所で、
  桁を揃える空白（行内コメントの前を含む）と流れ形式の並びを使っており、
  **書き戻しで詰められて `round-trip-changed` で落ちる**書き方でした。
  人は載っている書き方を真似るので、真似た人の図も落ちます

- **配置図で、動線が壁の下に隠れなくなりました。** 壁に厚みを付けたところ、
  避難経路の矢印が丸ごと壁の下に入りました。**避難経路図は矢印が主役**です（仕様 §3.0.4）

- **`zumen_spec` が、書ける場所をすべて返すようになりました。** `kind` / `at` /
  `size` / `tag` / `technology` / `openings` / `direction` / `wrap` を
  **1 つも載せていませんでした** —— エージェントが図を書く前に読む場所なので、
  載っていないキーは存在しないのと同じで、**誰も間取り図を書けない状態**でした。
  `kinds` / `directions` / `openings` / `sides` の語も返します
- **`zumen_inspect` の助言に `wrap` が入りました。** 横に伸びすぎた図に対して
  「分けられないか人へ聞く」しか言えませんでした

- **`validate` が、AI の書いた指定を見るようになりました。** `at` / `size` /
  `openings` / `kind` / `direction` / `wrap` を 1 つも見ていなかったため、
  `at: { x: "ひだり" }` も、構成図に書いた建具も、**黙って無視されていました**。
  すべて警告です（読める図なので止めません。仕様 §8.1）

- **Mermaid の向きが、正本どおりになります。** `flowchart TD` で固定していたため、
  **同じ正本から SVG は横、Mermaid は縦**が出ていました
- **Mermaid と draw.io で、符号（`tag`）と副題（`technology`）が落ちなくなりました。**
  書き出した時点で消えていました。`C1<br>柱<br>700×700` のように行を分けます

- **配置図でも、正本に書いた辺を描きます。** 一度「平面図に矢印は無い」として
  落としましたが、間違いでした。売場の補充動線が黙って消えていました。
  間取りに矢印が出ないのは**正本に辺が無いから**で、種類で落としているのではありません

- **配置図では、書いた座標が動きません。** 接している箱を重なりとして押し退けていました。
  間取りや売場では、部屋や棚が接しているのが普通です
- **配置図では、囲みが中身に合わせて縮みます。** 広げる向きにしか動かず、
  囲みどうしが重なっていました

- **ダークでは、箱が面になります。** 以前は黒地に白い輪郭だけで、線画のようでした。
  箱を一段持ち上げ、囲みを箱より沈めてあります

- **図の向きが横になりました**（既定）。`direction: down` で縦へ戻せます。
  縦長は流れ図に見え、画面にも資料にも収まりが悪いためです
- **箱と囲みが、地から見えるようになりました。** 枠が地に対して 1.47:1 しかなく、
  「きれいだが見えていない」図になっていました。**形（円柱・六角形・雲）も、
  これで初めて分かるようになります**
- **投影の測り方を直しました。** 長辺ではなく「**16:9 の画面からはみ出すほうの辺**」で測ります。
  長辺だと、横長にして実際に読みやすくなった図が「悪くなった」と出ていました

### 増えたこと

- **`kind: placement`** と **`nodes[].at`** — 配置図では、**置き場所を自分で書けます**。
  機械は並べ直しません。**`pins` には書かないでください**（人のものです）。
  `at` が無い要素は機械が置きます（黙って重ねません）。
  **物差しの向きは構成図と同じ**です（人が触った要素が少ないほど良い）
- **`nodes[].size`** — 箱の大きさを書けます（`{ w, h }`）。**構成図でも効きます。**
  間取りのように**大きさが意味を持つ図**では、書かないと全部同じ箱になります。
  **縮尺の仕組みは持ちません** — 題に「1 マス = 910mm」と書けば足ります
- **`direction: down`** — 図の流れる向き。省略すると `right`

- **`zumen_about`** — この道具が何で、何をしないか、版ごとに何が変わったかを返す。
  はじめに 1 回叩くと、**開いていない口を試して断られる往復が減ります。**

### 配り方

- **タグを打つと GitHub Release へ実物が貼られます** — Actions の Artifacts は
  GitHub にログインしないと落とせないため、外の人へ配るにはこちらが要ります
- **紹介のページ**（`site/`）を置きました。https://meta-taro.github.io/zumen/
  載せている図は**アプリが出した実物**で、絵として作ったものではありません
  （`site/gallery/` と CLI の出力が 1 バイトずつ一致することを確かめてあります）。
  **描いてある構成は例のために組んだもの**で、実在のものではありません

### 中で変わったこと（使う側には見えません）

- 版を上げる手順を `pnpm version:set <x.y.z>` にまとめた。
  **4 か所が揃っているかはテストが見張る**ので、手で直しても構いません

---

## 0.1.0 — 2026-09-08

**最初に配った版。**Actions の Artifacts から mac 版・Windows 版が落とせます。

### 描く

- **`type` が形になる** — `database` は円柱、`storage` は積み重ね、`internet` は雲、
  `cache` は六角形、`queue` は仕切り、`container` は二重枠。残りは矩形。
  **色ではなく形で意味を持たせる**ので、白黒でも縮小でも見分けが付きます
- **線を直角で引く** — 箱を避けて回り込みます。斜めの直線ではありません
- **箱の幅をラベルから決める** — 全角を半角 2 個ぶんとして測ります。上限で止めます
- **`technology` を副題として描く** — 版や所属を書ける唯一の場所です
- **辺のラベルは線の上に置く** — 重なるなら**出しません**。
  重なった文字は間違った文字として読めてしまうので、欠けるより悪いためです
- **人が動かした要素へ、辺が付いてくる** — `pins` で動かした先に線が届きます

### 検査する（`zumen_inspect`）

- **`hiddenLabels`** — 書いたのに絵に出ていないラベルの、辺の id。
  短くするか辺を減らしてください
- **`tooSmallToProject`** — 投影すると字が読めない大きさか（長辺の 1.5% が下限）。
  **文字を大きくして直そうとしないでください。**図が伸びて比がさらに下がります。
  図を分けられないかを人へ聞いてください
- **`reviewed`** — **人がこの図を見たか。**偽なら、まだ誰も確かめていません

### 書き出す

- **`theme`** — `dark` を渡すと、暗い地へ貼る用の色になります。省略するとライト
- **`intent`** — `vivid` で主役を強く出します。省略すると `safe`。
  **どちらでも、白黒で区別が消えないことは保たれます**

### 受け取り方

- **数字で書かれた値をそのまま受けます** — `technology: 22` のように
  引用符なしで書けます。以前はここで例外になっていました
- **改行は CRLF でも通ります** — Windows の Git が展開したものをそのまま読めます

### 開いていない口

**この版で開けていないものです。試しても断られます。**

- **競合の決着** — 開けると、AI が自分の提案を自分で承認できてしまいます
- **`pins` の書き換え** — 人が手で決めたことは、人のものです
- **「見た」の印を書くこと** — 画面からしか付けられません
- **既存ファイルの無条件な上書き** — `zumen_propose` を通してください
