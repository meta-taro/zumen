node_modules/
.env
.env.local
dist/
build/
coverage/
*.log
.DS_Store

# ローカル秘密控え（🟡 識別子のみ・🔴 token/key は置かない）— commit 禁止
SECRETS.local.md

# ベースルール完全版（内部事例を含むため公開しない）— commit 禁止
.claude/rules/product-baseline.full.md

# 画面のビルド成果物
app-dist/

# Tauri のビルド成果物
src-tauri/target/
src-tauri/gen/

# 画面写真を撮るときの包み（下書き場へ置く。過去に 4 枚こぼした）
examples/gallery/*.html

# **図の写し**（`pnpm pages` が `examples/gallery/` から写す。正本はそちらの 1 つだけ）
site/gallery/

# 見本を出して見るための PNG（qa/品質100周.md。見るためだけのもので、正本ではない）
qa/shots/
