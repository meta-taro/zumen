# 005 — 承認のための最小 GUI の範囲を決める（編集機能一式を作らない）

- Phase: 作る順 B3
- 前提: Issue 001 が Go / Conditional
- 優先度: 中

## 目的

**このプロジェクトが最も失敗しやすいのがここ。**

原案 §7 の v0 必須操作（選択・ドラッグ・リサイズ・接続・テキスト編集・削除・Undo・
Zoom / Pan・保存）は、**既存の作図ソフトの機能一覧と区別がつかない**。
これを揃えることを目標にすると、機能の少ない作図ソフトが出来上がり、
draw.io を採らなかった判断（D1）を自分で裏切ることになる。

GUI に要るのは編集機能そのものではなく、**AI の書き換えを人が承認できること**である。

- AI が何を書き換えたのかが見えること
- 人が動かしたものを AI が戻していないかが見えること
- 戻されていたら、そこだけ人の指定として固定できること

## 決めること（このリポで決めてよい）

- 上の 3 つを成立させるのに必要な操作は何か。**そこから逆算して機能を決める**
- 逆に、v0 で**やらない**操作を明記する

## 完了条件

- v0 の操作一覧が、**「承認のためにこれが要る」という理由付きで**書かれている
- やらない操作が列挙されている
- **見た目の完成度を完了条件にしていない**（汚くてよい）

## 注意

- **使いやすさの改善に時間を吸わせない。**ここが最大の失敗経路
- 機能を足したくなったら、Issue 003 の指標が改善するかで判断する。
  改善しない機能は、この製品の機能ではない

## 作業ログ

- 2026-09-06 着手。承認に要る 3 つから逆算する形にした。
  **原案 §7 の必須操作一覧を出発点にしない**（そこから引き算すると、
  「既存の作図ソフトから機能を削ったもの」にしかならない）

## 結果

**完了（決めるところまで。実装はまだ）。**

- 操作の一覧と理由: `docs/specs/005-承認のための最小GUI.md`
- 決定: `.claude/decisions.md` の **D11**（操作範囲）／**D12**（Tauri + Svelte）

### 完了条件との対応

| 完了条件 | 状態 |
|---|---|
| v0 の操作一覧が、理由付きで書かれている | 済。**8 個**。すべて承認の A / B / C のどれかに紐づけてある |
| やらない操作が列挙されている | 済。9 種。**Undo / Redo を作らない理由**も書いた（正本が Git にあるので二重管理になる） |
| 見た目の完成度を完了条件にしていない | 済。`DESIGN.md` は空のまま（**ベースルール §11。AI は埋めない**） |

### 人に決めてもらうこと

**`DESIGN.md` が空のままなので、実装に入る前に人が埋める必要がある。**
姉妹アプリ `md-business/apps/desktop/DESIGN.md` に同じ人が書いたデザイン正本があり
（Linear 風・トークン化・ライト主軸＋ダーク同等）、D1 の「既存シリーズに揃える」を
踏まえるなら**そこへ揃えるのが自然**だが、**揃えるかどうかは人の判断**。

実装側は、色や寸法を 1 か所のトークンに集めておく。
`DESIGN.md` が決まったら、そこだけ差し替えれば済むようにする（D9 と同じ作法）。
