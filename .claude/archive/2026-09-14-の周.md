# 2026-09-14 の周（作業の記録）

> `.claude/project-status.md` から退避したもの（ベースルール §24）。
> **消さずに残す。** なぜそう直したかは、ここにしか書いていない。
>
> この日は **1 日で 12 周**まわし、見本が **84 枚 → 100 枚**になった。
> 道具を足したのは 2 つだけ（`line: double`／`straddles` などの観測値）で、
> **残りはすべて「調べてから描く」で足したレパートリー**。

---


## 2026-09-14 の周

**レパートリー**（UI 方針 §10 の「最低限」を、これで全部埋めた）

| 見本 | 中身 |
|---|---|
| 85 Checkout の画面遷移 | Cart → Address → Payment → Confirm → Complete。**エラーの戻り先**つき |
| 86 CRUD 管理画面の画面遷移 | 一覧 → 作成／編集／削除確認。**削除は確認を挟み、保存後は必ず一覧へ戻る** |
| 87 モバイルアプリの UI 構造 | Bottom Navigation 5 タブ。**投稿だけモーダル**（タブを切り替えない） |
| 88 レスポンシブ LP の UI 構造 | Desktop / Tablet / Mobile。**節が消える・増える**ことを表で言う |

**7 件すべて、道具を足さずに描けた**（UI 方針 §9 のとおり）。

**ブラッシュアップ** —— `zumen_inspect` に `overlappingText`（文字どうしの重なり）。

- きっかけは実害。86 で注記の箱を動かし忘れ、**枠の上に文が乗ったまま**出ていた。
  `crowdedNames` も `hiddenLabels` も `crossings` も 0 —— **どれも見ていない所だった**
- `overlaps`（箱）は入れ子を数えるので配置図では鳴りっぱなしで、誰も見ない。
  **箱の重なりは意図のことがあるが、文字の重なりはほぼ必ず間違い**なので分けた
- 入れて回したら、**既にある見本 3 枚で実際に重なっていた**
  - 43 舗装構成断面図 … 表層・基層は 50 mm ＝ 10 px。**縮尺を崩さず、名前を右の構成表へ出した**
  - 74 EC 商品ページ … **器の名前を器の真ん中に書くと、中身の名前に乗る。**
    Header・Product Info の名前を端の欄へ出した
  - 86 … 注記を下へ移した
- 見本 88 枚すべてで 0 件を回帰テストで押さえた（`test/names.test.ts`）

**門**：typecheck / test 949 件 / coverage 98.62・93.50・97.41 / `gallery:check` / 個人情報検査、すべて通過。

---

## 2026-09-14 の周（2 周目）

**レパートリー** —— 見本 89 コンテナ船の積付図（Bay Plan）。

- **線が 1 本も無い図。** セルの位置がそのままコンテナの住所（ISO 9711-1）
- 調査は `.claude/sample-ideas.md` の末尾に 13 項目で残した。英語でも掘った
- **道具は足していない**（`at` ＋ `palette` ＋ `tag` ＋ `hatch` だけ）

**ブラッシュアップ** —— `color-without-code` の見る場所を、節から**図ぜんたい**へ。

- 前は「その節の `tag` が鍵で始まっているか」。穴が 2 つあった
  - **`tag` の無い節では一度も鳴らなかった**（符号がどこにも無いのがいちばん危ない）
  - **`tag` が別の意味を持つ図で誤って鳴った**（積付図の `tag` はリーファーと危険物の印）
- 実物の路線図も、駅ごとに色名は書かない。**凡例に 1 回書いてある。**
  だから「その鍵が図のどこかに文字として出ているか」だけを見る
- あわせて**見本 4 枚の `palette` の鍵を、読み手が見る言葉に変えた**
  （`tokkyu` → `特急`、`heijitsu` → `平日`）。鍵が内部の名前のままだと、
  色の意味が正本を読んでも分からない

**GitHub Issue** —— #9（`type` が SVG で形にならない）を実測つきで報告して close。
形を付けたのは 11 種のうち 6 種で、**残りは矩形のままにした理由も書いた**。

**門**：typecheck / test 952 件 / `gallery:check` / 個人情報検査、すべて通過。

---

## 2026-09-14 の周（3 周目）

**レパートリー** —— 見本 90 相続関係説明図（法務局へ出す書面）。

- 調べたのは「家系図の描き方」ではなく、**補正（差し戻し）になる条件**。
  いちばん多いのは戸籍との不一致（旧字体・異体字・生年月日・住所・本籍）
- 誰が不動産を取るかを（相続）（分割）で図の上に書く。**書かないと補正になる**
- **相続放棄した人も省かない。** 省くと相続人の範囲が戸籍と合わなくなる
- 代襲は（被代襲者）（代襲者）。**被代襲者の死亡年月日が要る**

**道具を 1 つ足した（D27）** —— `edges[].line: double`（二重線）。

- **婚姻は二重線、親子は単線**と決まっていて、太さでも破線でも代わりにならない
- 足したのは**線の形の名前**で、意味の語（`marriage`）ではない。D22 の線引きどおり
- 描き方は「太い線の上に地の色の細い線を重ねる」。折れ線でも曲線でも効く

**分かったこと** —— **子は父からではなく、夫婦の関係から降りる。**
婚姻の線の真ん中に点（`marker: none`）を置いて、そこから子を降ろした。
父の箱から線を引くと斜めになり、実物と違う絵になる。

**門**：typecheck / test 955 件 / `gallery:check` / 個人情報検査、すべて通過。

---

## 2026-09-14 の周（4 周目）

**レパートリー** —— 見本 91 リンゴ高密植栽培の樹形図（トールスピンドル）。

- 縮尺つき（1 px = 10 mm）。樹高 3.2m／樹間 0.9m／列間 3.0m
- **下垂誘引が記法。** 枝が下を向いているのは絵の都合ではない
- 切り返し（途中で切る）と間引き（基部から切る）を図の上で区別
- 調査 13 項目は `.claude/sample-ideas.md` の末尾

**ブラッシュアップ** —— **`validate` が、置いたあとの形も見るようにした**（`text-overlap`）。

- **文字どうしの重なりは、正本だけを読んでも分からない。**
  先週入れた `overlappingText` は `zumen_inspect` にしかなく、
  **人が CLI で確かめられなかった**
- 見本 86 を壊して確かめた ——
  `e_r3 と note の文字が重なって描かれます` が出る
- 交差は出さない（**合否ではなく観測値**。自己採点させない）

**積み残し（人へ渡す判断）** —— **線を引くためだけのノードが、見本全体で 49 個ある**
（91 で 15 個、71 で 12 個、83 で 9 個）。辺は必ず節と節を結ぶので、
**枝や仕掛けの糸のような「ただの線」を引く手段が無い。**
直し方は 2 通り（辺の端に座標を許す／線という別の節を足す）。
**どちらも形式の変更**なので、この周では足していない。

**門**：typecheck / test 958 件 / `gallery:check` / 個人情報検査、すべて通過。

---

## 2026-09-14 の周（5 周目）

**レパートリー** —— 見本 92 防虫モニタリングの定点配置図（HACCP / IPM）。

- 虫を殺す図ではなく、**月どうしを比べる図**。だから**定点を動かさない**
- 外の定点に措置値を置かない（外に虫が居るのは当たり前。見るのは中との差）
- ライトトラップは製品ラインの真上に置かない（捕まえた虫が落ちる）
- 調査 13 項目は `.claude/sample-ideas.md` の末尾

**ブラッシュアップ 2 つ。**

1. **`zumen_spec` の見本に、足した語が載っていなかった。**
   `line: double`（D27）も `marker` の `ellipse` / `diamond` / `bar` も**一度も**。
   **そこに無い語は、エージェントにとって存在しないのと同じ。**
   語彙の一覧と見本が食い違わないことをテストで見張るようにした
2. **入らない符号を、黙って落とさない**（`hiddenTags` / `tag-hidden`）。
   見本 92 で 22px の印に書いた `LT-1` が 1 つも出ておらず、
   **「番号の無い丸が 14 個」**になっていた。入れて回したら
   **既にある見本 2 枚でも符号が消えていた**（26 のラック列・27 の水路）

**門**：typecheck / test 963 件 / `gallery:check` / 個人情報検査、すべて通過。

---

## 2026-09-14 の周（6 周目）

**レパートリー** —— 見本 93 花火大会の打揚配置・保安距離図。

- **円が図の中身**で、飾りではない。号数ごとに保安距離が決まっている
  （火薬類取締法施行規則 → 県の技術基準）
- 測り方は**消費場所から通路等までの水平最短距離**
- 強風（10 m/s 以上、または強風注意報）では打ち揚げない
- 調査 13 項目と距離の表は `.claude/sample-ideas.md` の末尾

**ブラッシュアップ** —— **土木の図が、ミリで書かれていた。**

- 200 m の円に `R=200,000`。ダムに `200000`、圃場整備に `125000`、仮設に `60000`。
  **桁を数えないと読めない数字は、書いていないのと同じ**
- **単位は縮尺が決める**（`src/units.ts`）。1 px が 100 mm 以上なら m
- **数値の大きさでは決めない** —— 同じ図に `45 m` と `5,000` が並ぶ
- あわせてミリも 3 桁区切りにした（`3600` → `3,600`）。実物の図面はそう書く

**門**：typecheck / test 968 件 / `gallery:check` / 個人情報検査、すべて通過。

---

## 2026-09-14 の周（7 周目・人の指摘から）

**オーナーの指摘。**

> LP のサンプル図がおおくなったので、UI/UX 考慮にいれてください。分類など。
> 順番もなんかてきとうっぽかったので、路線系がはなれている？など。

**そのとおりだった。** 並びは**足した順**に近く、路線図が 3 か所に散っていた
（25 / 45 / 52〜54 / 71 / 78〜83）。分類も 7 つで、
「組織・業務」に囲碁・将棋・チェス・ビンゴ・舞台照明・コンテナ積付が入っていた。

**直したこと。**

| | 前 | 後 |
|---|---|---|
| 分類 | 7 つ（業界の粗い括り） | **9 つ**（鉄道・交通 18／建築・土木 14／設備・電気 8／医療・食品・農 9／店舗・物流 8／業務・組織 12／催し・遊び 7／UI・画面設計 7／IT・ソフトウェア 10） |
| 並び | 足した順に近い | **分類ごとにまとめ、区切りの見出しを出す** |
| 絞り込み | 上に 1 回だけ | **上に貼り付く**（93 枚あるので、選び直しに先頭へ戻らせない） |
| 件数 | 出していない | **ボタンと見出しに出す** |

**ページの組み立てを機械に渡した**（`scripts/gallery.mjs`）。

- 見本を足して**ページに足し忘れる**事故が毎回起きていた。
  `pnpm gallery:check` が落とすようにした
- **絵の大きさを SVG から書く**ので、遅延読み込みでも下の図が飛ばない
  （実機で確かめた。93 枚を lazy にしたら、読み込むたびに図が飛んでいた）
- 並びと分類の正本は `scripts/gallery-categories.mjs`

**実機で 2 件見つけて直した。**

1. `display: flex` は `hidden` より強く、**絞り込んでも見出しだけが並んでいた**
2. 大きさを書かずに lazy にしたので、**読み込むたびに下の図が飛んでいた**

**門**：typecheck / test 968 件 / `gallery:check` / 個人情報検査、すべて通過。

**同じ周のつづき。**

- **見本 94 ヘアカラーのブロッキング図。** 塗る順が図の中身
  （染まりにくいネープから、体温で早く進む顔まわりを最後に）。**道具は足していない**
- **狭い画面で、絞り込みを 1 行にして横へ流すようにした。**
  実物のボタン幅を測ったら、**390px では 5 段に折り返して 219px**
  —— 800px の画面の 27% を、貼り付いたまま占めていた。1 行なら 33px
  （**390px での描画そのものは、この環境では確かめられていない。**
  測ったのはボタンの幅と折り返しの段数）
- **新しく入れた `text-overlap` が、自分の間違いを 3 件すぐに捕まえた**
  （見本 94 の表の列幅）。入れる前なら、そのまま見本になっていた

---

## 2026-09-14 の周（8 周目）

**レパートリー** —— 見本 95 避難所レイアウト（体育館 30 × 20m・48 名）。

- **面積と間隔が図の中身。** スフィア基準（1 人 3.5m²、トイレ 20 人に 1 基・
  男女比 1:3、水 1 人 1 日 15L、蛇口 250 人に 1 つ、通路 1〜2m）
- **発熱者と濃厚接触者は、通路も階段も分ける**
- **「入れる人数」ではなく「居られる人数」**
- 調査 13 項目は `.claude/sample-ideas.md` の末尾

**ブラッシュアップ** —— **鳴りっぱなしの指摘を、言い方ごと直した。**

- `tooSmallToProject` が**見本 95 枚のうち 36 枚（38%）**で真だった。
  路線図・査定図・仕込図・積付図 —— **分けようのない図ばかり**で、
  毎回そのまま流していた。**読まれない指摘は、無いのと同じ**
- 測り直したら、**その 36 枚は全部 A3 の下限（0.84%）を通っていた。**
  言うべきことは「小さすぎる」ではなく「**印刷して読む図**」だった
- `tooSmallToPrint` を足し、**両方が真のときだけ「直すところがある」**とした。
  見本の実測は **投影に向かない 36 枚／印刷でも読めない 0 枚**

**門**：typecheck / test 975 件 / `gallery:check` / 個人情報検査、すべて通過。

---

## 2026-09-14 の周（9 周目・夜）

**レパートリー** —— 見本 96 オーケストラの配置図（対向配置と現代配置）。

- **同じ楽器を、どう並べるかだけの図。** 動くのは第 2 Vn と Vc（と Cb の側）だけ
- 対向配置は第 1 と第 2 の掛け合いが左右に分かれて聞こえる（古典派はそう書かれている）
- 弧に並べるのは飾りではなく、**指揮者から等距離**にするため

**ブラッシュアップ** —— **`straddles`（はみ出して重なっている箱）。**

- 今夜 3 回とも、手書きのスクリプトで同じ確認をしていた。**道具に入れた**
- `overlaps` は入れ子も数えるので配置図では鳴りっぱなし（400 組超）で、誰も見ない。
  **直すところがあるのは、どちらも相手を含んでいない重なり**
- 入れて回したら、**見本 10 枚に本物の間違いが埋まっていた**
  - 15 店舗 … **冷蔵ケースと弁当什器が、床の同じ場所を取っていた**
  - 30 仮設 … 出入口が建物へ食い込み、**クレーンが車輌通路を塞いでいた**
  - 93 花火 … **消防車が立入禁止区域へはみ出していた**
  - 81 地下鉄 … 終点の行き先が 2 つ重なっていた
  - 91・92・94・95 … 見出し・注記・定点が他のものの上に乗っていた
- わざと重ねている図（伏図の柱、断面の水抜管、碁石、舞台のバトン、
  棚のワイヤー、壁沿いの粘着トラップ）は**理由を書いて外した**（`crossings` と同じ扱い）

**ついでに測ったこと** —— **書き出しは 96 枚 × 4 形式（Mermaid / draw.io / Markdown / SVG）
すべて通る。** 落ちるものも、中身が欠けるものも無かった。

**門**：typecheck / test 976 件 / `gallery:check` / 個人情報検査、すべて通過。

---

## 2026-09-14 の周（10 周目・夜）

**レパートリー** —— 見本 97 防犯カメラの視野図（DORI つき）。

- **「写っている」と「誰か分かる」は別。** DORI（IEC/EN 62676-4）で段階を分ける
- 距離は 画素数 ÷（2 × 距離 × tan(画角/2)）。
  **画角を広げるほど、識別できる距離は縮む**（4MP で 70°→7.3m、90°→5.1m）
- 出入口は**内向き**に撮る（外向きは逆光で顔が飛ぶ）
- **扇形は道具を足さずに描けた** —— 自分自身への辺 ＋ `via` ＋ `close: true`

**ブラッシュアップ。**

- **`via` の無い自分自身への辺が、長さ 0 の線に矢印だけを出していた。**
  節の真ん中に黒い粒が出るだけで、**知らせるものが無かった**（`edge-self-open`）
- **閉じた形の書き方を `zumen_spec` の見本と仕様に書いた。**
  書いていない書き方は、エージェントにとって存在しないのと同じ

**門**：typecheck / test 979 件 / `gallery:check` / 個人情報検査、すべて通過。

---

## 2026-09-14 の周（11 周目・夜）

**レパートリー** —— 見本 98 パレット積付図（T11・4 つの積み方）。

- **同じ箱を、どう並べるかだけの図。** それで荷崩れが変わる
- 段ごとに向きを変えると横の力に強くなる（ブロック積みは目地が縦に揃う）
- **効率と安定はぶつかる** —— 風車積みはいちばん強いが真ん中が空く（88.7%）
- はみ出しは厳禁。わずかでも下段の圧縮強度が落ちる

**ブラッシュアップ** —— **README の数を、機械に数えさせるようにした。**

- 一晩で見本の枚数を 3 回手で直し、検査の件数は **1 件ずれたまま**だった
- **古い文書は、無い文書より悪い**（ベースルール §10）。
  手で数える限りまたずれるので、テストで止める（`test/docs-numbers.test.ts`）

**門**：typecheck / test 981 件 / `gallery:check` / 個人情報検査、すべて通過。

---

## 2026-09-14 の周（12 周目・夜）

**レパートリー** —— 見本 99 法廷の配置図（刑事・裁判員裁判）。

- **席がそのまま役割**の図
- **「右陪席」「左陪席」は裁判長から見た向き。** 傍聴席から見た図では左右が逆
- **検察官席と弁護人席の左右は、法律で決まっていない**（裁判所によって違う）
- **裁判員は法壇に座る**（傍聴席側ではない）

**ブラッシュアップ** —— **手直し保持を、見本ぜんたいで測るようにした。**

- S1 は本物の AI で 10 回まわして 10/10 だったが、**それは特定の 1 枚**の話。
  路線図・伏図・積付図・視野図 —— **形が変われば壊れ方も変わりうる**
- API を使わずに往復を模す ——
  人が 1 か所だけ位置を直し、**AI は手直しを知らない提案を出す**
- 実測：**99 枚すべてで手直しが残り、競合は 0 件**
- あわせて、**紙の余白ぶん図ぜんたいが平行移動する**ことが分かった
  （通り芯や方位のある図）。座標そのものではなく
  「人が動かした先へ箱が付いていったか」で見ている

**門**：typecheck / test 982 件 / `gallery:check` / 個人情報検査、すべて通過。
