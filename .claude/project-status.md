# プロジェクトステータス — zumen

- **現在フェーズ**: 測る順 S1（Issue 001）着手中
- **最終更新**: 2026-09-04

## 完了した作業
- **S1 の判定基準を確定**（`docs/specs/s1-判定基準-手直しの保持.md`）。
  Issue 001 の作業内容 1「判定基準を実装より先に書く」に対応。
  保持対象を Tier A / B / C に分け、Tier A は 100% 保持を合格線とした。
  競合は検出・選択・持続の 3 項目すべてを必須とし、黙って上書きした時点で No-Go とした。

## 未完了の作業
- **Issue 001 の往復実験**（作業内容 2〜4）。実装はまだ無い
- Issue 002（保存形式 D2 の決定案）

## 次のタスク
1. 往復実験の実装 — 実験用の暫定形式・決定的な書き換え・保持/反映/競合の測定
2. 往復 3 回以上（R0〜R4）を回し、記録を残す
3. Go / Conditional / No-Go の判定を出す

## 技術的決定
- `.claude/decisions.md` を参照（現時点で確定は D1 のみ。**D2 / D3 は未決**）
- S1 は D2 を決めない。実験用の暫定形式の上で測り、必要と分かった保持要件を Issue 002 へ渡す

## テスト状況
- **未整備。** 実装がまだ無いため。
  判定基準 §8 のとおり、主要測定は外部サービスに繋がらない形で走らせる（ベースルール §4）

## 既知の問題
- `gh repo view` の `visibility` が **PRIVATE**。CLAUDE.md は public 前提で書かれている。
  公開に切り替えるのは人の判断。切り替え前提でベースルール §25（個人情報を残さない）は今から守る
- GitHub Issue は 0 件。作業は `.claude/issues/` のローカル Issue で駆動している
