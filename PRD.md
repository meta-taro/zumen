# PRD — zumen（図面）

> **叩き**。ここで決めるのは方向性と最初の一歩までで、実装はこのリポで行う。
> 原案は `docs/origin/` に原文のまま置いてある。**この PRD は原案の要約ではなく、
> 原案から何を動かしたかを書いた文書**である。動かしていない部分は原案を読むこと。

## §0 原案の扱い

受領した原案は 2 本ある。**競合案ではない。**

| ファイル | 位置づけ |
|---|---|
| `docs/origin/00b-壁打ち-drawio-mcp統合検討.md` | 先行する検討。draw.io を部品として使う案 |
| `docs/origin/初期の企画草案` | その検討を経た結論。独立 OSS として作る案 |

`00b` は却下された案だが**捨てない**。「なぜ draw.io を使わなかったのか」は必ず後から聞かれる。
判断とその理由は `decisions.md` の D1 に切り出してある。要点だけ再掲する。

- 却下理由 1 — **そこまでの高機能を必要としないケースが多い。** 描きたいのはサーバー・
  ネットワーク・クラウド構成図で、そこで使う語彙は多くない。機能の大半は使われないまま、
  組み込みの制約とその機能を前提にした UI だけが残る
- 却下理由 2 — **デスクトップ版を持ち、既存シリーズに揃えるほうが意義がある**

**判断軸は「できるか」ではなく「必要か」だった。** これは以降の判断すべてに効く。

## §1 誰のためか

**構成図を描かされている人**である。図を描くのが仕事の人ではない。

- サーバー構成・ネットワーク構成・クラウド構成を、**説明のために** 描く必要がある
- 図形ツールの操作を覚えたいわけではなく、**説明できる絵が欲しい**だけ
- 構成は変わる。**変わるたびに描き直すのが苦痛**で、たいてい図は古いまま放置される

**この 3 つ目が本体の課題である。**「白紙から描くのが大変」ではなく「**更新されないまま腐る**」。
既存の作図ソフトは 1 枚目を描く道具としては完成しており、そこを競っても勝ち目は無い。
勝負するのは **2 枚目以降**――つまり「構成が変わったので図を直す」場面。

## §2 原案から動かした点

### 動かした点 1 — MVP を「エディタ」ではなく「1 枚の往復」にする

原案 §19 は Phase 0（Format）→ 1（Renderer）→ 2（Minimal Editor）→ 3（AI）と積む。
**この順だと、AI が出てくるのが 4 番目になる。** AI がこのプロダクトの中核である以上、
**中核が最後に来る順序は採らない。** Phase 2 まで作った時点で手元にあるのは
「機能の少ない作図ソフト」で、それは世の中に既にたくさんある。

最初に作るのは次の 1 本の線である（原案 §27 の成功条件を最小化したもの）。

```text
文章で頼む → 図が出る → 人が 1 か所だけ直す → もう一度文章で頼む → 直した分が壊れない
```

**最後の「壊れない」が唯一の勝負どころ。** ここが成立しないなら、他が全部できても
このプロダクトを使う理由が無い（原案 §26 の 2・6 が同じことを言っている）。

### 動かした点 2 — GUI を「編集の場」ではなく「差分の見える場」として設計する

原案 §7 は選択・ドラッグ・リサイズ・接続・削除・Undo を v0 必須としている。
**この一覧は既存の作図ソフトの機能一覧と区別がつかない。**
必要なのは編集機能そのものではなく、**AI が書き換えた結果を人が承認できること**。

- 人が動かしたものを AI が勝手に戻していないか、が見えること
- 戻されていたら、そこだけ人の指定を優先として固定できること

編集機能は、この承認を成立させるのに要る分だけ実装する。

### 動かした点 3 — md-business 連携を最後に置かない

原案では Phase 5。**姉妹プロジェクトである以上、連携は後付けの機能ではなく前提**であり、
後ろに置くと「単体で完結する別アプリ」として作り込まれてしまう。
ただし **md-business の機能として作らない**（単体で使えることは維持する）。
最初から意識するのはファイルの置き方と CLI の口であって、統合機能ではない。

## §3 最初に作るもの（スコープ）

**成果物は「アプリ」ではなく「判断材料」。** 下記が取れた時点で一度止めて、続けるか決める。

- 構成図 1 枚を、文章の指示から生成できる
- 生成物はテキスト（人が読める・Git の差分が読める）
- 人が 1 か所を手で直せる
- 直した状態で再度指示を出し、**手直しが保持される**
- 図を SVG で出せる

**この範囲に GUI の完成度は含まれない。** 汚くてよい。

## §4 やらないこと

原案 §20 に加えて、叩き段階で明示的に足したもの。

- **図形の網羅を追わない。** 最優先の対象図（原案 §4）以外の形は当面持たない
- **リアルタイム共同編集をやらない。** Local-first と両立させる設計コストが本体価値を食う
- **Web 版を先に作らない。** デスクトップ版に揃えるのが D1 の判断そのもの
- **他形式のインポートを最初に作らない。** 入口を広げると、この製品の出力品質が
  他所のデータ品質に引きずられ、勝負どころが測れなくなる

## §5 UX チェックリスト §0 への現時点の回答（OSS 版）

`.claude/templates/ux-checklist/product-ux-checklist-ja.md` の 5 問。**OSS なので 4 問目は置換する。**

| 問 | 現時点の答え |
|---|---|
| 誰の、どんな面倒を消すのか | 構成図を**描き直す**面倒。描き始める面倒ではない |
| なぜ今の方法をやめてこれを使うのか | 既存ツールでは更新のたびに人が図形を動かす。ここでは文章で言えば直る |
| AI が 10 倍賢くなっても必要か | **必要。** 賢くなるほど生成は易しくなるが、**人が直した意図を保持する**のは別問題で、AI 性能では解けない |
| 作った人がいなくなっても続くか | **未確定。保存形式に全部かかっている**（D2）。テキスト正本・仕様が実装から分離、が条件 |
| 目的そのものを壊していないか | 図は「理解を共有するため」に描く。全自動で出るだけの図は誰も理解しないまま貼られる。**人が直す往復を残すこと自体が目的**であって、往復を消す方向へ進めてはいけない |

## §6 未解決（これが決まるまで実装に入れない）

- **D2 — 保存形式。** 4 問目の答えがここに全部かかっている。独自形式にすると
  この製品が終わった時点で図が読めなくなる。**D1（draw.io を採らない）と
  「draw.io 形式で保存する」は両立する**ことに注意（部品として使うかと、
  出力互換を持つかは別の問題）
- **D3 — 何をもって「AI が 9 割描けた」と言えるのか。** 測れないと、
  出来上がりが既存の作図ソフトと同じになっていても気づけない

## §7 名前

**`zumen`（図面）。**AI 単独判断で置いた仮の名前。

- **図面は「更新され続けるもの」**であり、この製品が向き合う課題
  （構成図が更新されないまま腐る）に語がそのまま重なる
- 3 音・ローマ字で、姉妹の OSS 群（割符・襷）と並ぶ
- 初版は `sashizu`（指図）だったが差し替えた。建築由来の
  「図面そのもの」の意で選んだが、**現代語では「指図する」の
  命令の含みが先に届く**。製品名は意味より語感が先に届くので採らない
- 次点は `mitorizu`（見取図）＝意味は最良だが 8 文字と長い、
  `ezu`（絵図）＝短いが漠然

**リポジトリ作成は名前が確定してから行う**（作成は公開行為）。
異論があればここで差し替える。差し替えのコストは今なら `git mv` 1 回。
