# DESIGN.md — zumen

> **デザインの出典は姉妹アプリ。**
> `md-business/apps/desktop/DESIGN.md` と `src/lib/styles/tokens.css` を正本とし、
> zumen はそこへ揃える（2026-09-06、人の指示。D1 の「既存シリーズに揃える」の実行）。
>
> **ここに書くのは、揃えるだけでは決まらない zumen 固有のことだけ。**
> 色の値・書体・余白・角丸・影・モーションを zumen が独自に決めることはしない。
> 姉妹側が変わったら、こちらも追う。

---

## 1. 揃えるもの（姉妹アプリの正本を参照する）

| 決めごと | 出典 |
|---|---|
| カラートークン（ライト主軸＋ダーク同等） | 姉妹 DESIGN.md §2 / `tokens.css` |
| タイポグラフィ（ベース 14px、`--text-*`） | 姉妹 §3 |
| スペーシング（4px グリッド）・角丸・影・モーション | 姉妹 §4 |
| ボタン・カード・入力・バッジ | 姉妹 §5 |
| **静けさ優先／余白で階層／繊細なアクセント／速いフィードバック／キーボードファースト** | 姉妹 §1 |

**コンポーネントはハードコード色を使わない。** トークンだけを参照する（姉妹 `tokens.css` の約束）。

---

## 2. zumen 固有 — 図そのもの

姉妹アプリに「図のキャンバス」は無いので、ここだけは zumen が決める。
**ただし新しい色を作らない。** 姉妹のトークンから意味を借りる。

### 2.1 図の要素

| もの | 使うトークン | なぜ |
|---|---|---|
| ノードの地 | `--bg-app` | 図の主役は関係であって箱ではない。箱は静かに |
| ノードの枠 | `--border-strong` | 罫線を増やさず、余白で読ませる（姉妹 §1 の 2） |
| ノードの文字 | `--text-primary` | |
| 囲み（VPC 等）の地 | `--bg-subtle` | 従属面。中身より一段沈める |
| 囲みの枠 | `--border` + 破線 | 「区画であって物ではない」ことを線種で示す |
| 囲みのラベル | `--text-secondary` | |
| 線（エッジ） | `--text-secondary` | **枠の色を使わない。** ヘアライン用の薄さなので、線に当てると地に沈む（実際にそうなった）。**図の主役は関係**なので箱の枠より濃く |
| 線のラベル | `--text-secondary` | |

### 2.2 `appearance`（正本に書く意味の語。仕様 §4）

**正本には色を書かない。** 語だけを書き、色は描き手が決める。

| 語 | 地 / 枠 | 使うトークン |
|---|---|---|
| `primary` | 淡いアクセント地 / アクセント枠 | `--accent-subtle` / `--accent` |
| `muted` | 中立地 / 薄い枠 | `--neutral` 地 / `--text-tertiary` |

**アクセントは「説明の主役」1 点にだけ使う**（姉妹 §1 の 3）。
図の中で `primary` を多用すると、どれが主役か分からなくなる。

### 2.3 **人の指定であることの印**（D11 の操作 8）

**この製品でいちばん大事な視覚要素。**
これが見えないと、AI が人の指定を戻したことに気づけない（PRD §2 の動かした点 2）。

- **枠を 2px にする**（自動配置は 1px）
- 図の中では**色を変えない。** 色で示すと `appearance` と混ざり、
  「人が青くした」のか「人が置いた」のかが区別できなくなる
- 画面（GUI）では、選択中の要素に限り左端に `--accent` の 2px バーを添える
  （姉妹 §5.7 のサイドバー選択と同じ作法）

**太さで示し、色で示さない。** これが zumen 固有の一線。

### 2.4 競合（D11 の操作 7）

| 状態 | 使うトークン |
|---|---|
| 人の指定と提案が食い違っている | `--warning` |
| 人が「自分の指定を採る」と決めた（`locked`） | `--neutral` |
| 提案が人の指定した要素を消そうとした | `--danger` |

**競合は赤で埋めない。** 大半は「どちらも正しいが、どちらかを選ぶ」だけで、
危険なのは最後の 1 つ（消える）だけ。

### 2.5 差分（D11 の操作 6）

適用前の差分は**テキストの差分として見せる。** 図の上に重ねない。

- 図に重ねると、変わっていない部分と変わった部分の境目が読めない
- **正本はテキストで、`git diff` が読める形にしてある**（D2）。
  同じ見え方にすれば、画面で見たものと Git で見るものが一致する

---

### 2.6 図を置く紙は、テーマで変えない

図の色は書き出しと同じ値（§3）で、**ライト固定**。
地だけダークにすると、**画面の図と書き出した図が違って見える。**
この製品で持ち出されるのは書き出したほうなので、画面をそちらへ合わせる。

**枠（ヘッダ・右の欄）はテーマに従う。** 紙だけが固定。

---

## 3. 書き出した図の色（**CSS 変数を使えない**）

SVG や draw.io の書き出しは、**貼り先に持ち出される**（Issue 007）。
貼り先に zumen の CSS は無いので、**書き出しには色の値を直に書くしかない。**

そこで、

- **トークンから値への対応表を `src/tokens.ts` の 1 か所に置く**
- 書き出し（`render` / `mermaid` / `drawio`）は、そこだけを参照する
- **既定はライト。** 貼り先の地の色が分からないときは、常にライトの値を使う

**3 か所に同じ色を書かない。** 以前はそうなっていて、ズレる寸前だった。

### 3.1 貼り先が名乗るなら、ダークも出す（2026-09-07 に改めた）

**以前はここに「ダークテーマの値は書き出さない」と書いてあった。**
理由は「貼り先の地の色が分からない」から。

**その理由は、貼り先が自分で名乗るなら当たらない。**
姉妹アプリは自分がダークかを知っていて、それを渡してくる（md-business#240）。

> 背景を敷く案は採りません。敷いても図自身の線と枠はライトの値のままなので、
> 暗い地の上に明るい線が浮いた状態が残ります。板が 1 枚増えて見えるだけです。

そのとおりなので、**呼ぶ側がテーマを渡せる**ようにした。

```ts
toSvg(zumenSource, { theme: 'dark' })   // 省略すればライト
```

```bash
pnpm svg <図> [書き出し先] --dark
```

**変えていないこと。**

- **既定はライトのまま。** 名乗らない貼り先の見え方は 1 mm も変えない
- **ダークの値も姉妹アプリの `tokens.css` から取る**（`:root[data-theme='dark']`）。
  zumen が独自に決めた色はここにも無い
- **ライトとダークで同じ名前をすべて持つ。** 片方にしか無い色を作ると、
  切り替えたときにその要素だけ浮く（`test/theme.test.ts` が見張る）
- **SVG にだけ効く。** draw.io は貼り先の道具が自分の色を持ち、
  mermaid は自前のテーマを持つ。ここで決めると、あちらの設定と二重になる

---

## 4. ここで決めないこと

- **図の中のアイコン。** クラウド各社の公式アイコンは同梱しない（D7）。
  図形は自作の汎用形だけで、種別ごとの描き分けはまだ持っていない
  （**アプリのアイコンは §6 で決まっている**。別のもの）
- **図の中の書体。** 書き出した図は貼り先が書体を決めるので、
  総称ファミリだけを書く（Issue 007 §3.1）。**画面の中では姉妹のトークンに従う**
- **v0 の GUI に無い画面。** 操作は 8 つに限ってある（D11）

---

## 5. 姉妹側が変わったときの追い方

1. 姉妹の `tokens.css` の差分を見る
2. `src/tokens.ts` の対応表を合わせる
3. **`test/tokens.test.ts` が 3 つの書き出しの食い違いを見張る**

**追わないと決めた場合は、この文書に理由を書く。**
黙って離れると、2 つのアプリが少しずつ似ていない見た目になる。

---

## 6. アプリのアイコン（**人が決めた**。2026-09-07）

**配色の指示は人から出たものです。** AI が埋めた既定値ではありません（ベースルール §11）。

> ダークグレーにワインレッドの配色がいいかも

| | 値 | |
|---|---|---|
| 地 | `#23232b` | ダークグレー |
| 主役の 1 つ | `#a32b48` | ワインレッド |
| ほかの箱と線 | `#d8d8e0` | |

### 形

**構成図そのもの。** 1 つから 2 つへ分かれる木で、**分かれた先の片方だけがワイン。**

ワインを 1 点にだけ使うのは、図の中の作法（§2.2「アクセントは説明の主役 1 点にだけ」）と同じです。
アイコンの中で 2 つ以上をワインにすると、どれが主役か分からなくなります。

**16px まで縮めて読めることを確認して選んであります。** 4 案を 160 / 48 / 24px で並べ、
24px でも「箱と線の図」に見えたのはこれだけでした。

### §1 との関係

§1 は「色の値を zumen が独自に決めない（姉妹アプリに揃える）」と書いています。
**アプリのアイコンはその例外です。**

アイコンは**製品を見分けるための印**で、姉妹アプリと同じ色にしたら見分けがつきません。
姉妹側の `--accent`（`#5b5bd6`）はアイコンにも使われているため、揃えると 2 つのアプリが
Dock で同じに見えます。**ここだけは、違うことに意味があります。**

**UI の中では引き続きワインレッドを使いません。** ここはアイコン専用の色です。

### 作り直し方

**正本は `app/icon.svg` の 1 つだけ。** PNG / ICNS / ICO は出力なので直接いじらない。

```bash
pnpm icon
```

---

## 7. 映えと安全（**人が決めた**。2026-09-08。Issue #6）

Issue #6 は「ぶつかったときどちらが勝つかを先に決めてほしい」という提案だった。
**オーナーの判断は「どちらも勝たせない」。**

> ばえと安全は、人が操作によって、ないし AI エージェントに伝えることによって、
> **個々で選択できることが望ましい**

### 選べるもの

```ts
toSvg(source, { intent: 'vivid' })   // 省略すれば safe
```

```bash
pnpm svg <図> [書き出し先] --vivid
```

MCP は `zumen_export` の `intent`。**人の操作からも、エージェントへの指示からも届く。**

| | |
|---|---|
| `safe`（既定） | 主役を淡い地とアクセントの枠で示す。資料に混ぜても本文を邪魔しない |
| `vivid` | 主役をアクセントで塗り潰す。遠くから見る場に向く |

### **選べないもの — 白黒で区別が消えないこと**

**「読めなくてよい」は選択肢に入れない。**

これは安全を勝たせているのではなく、**そこに勝ち負けが無い**から。
色を足しても、形で区別する道は何も失わない。**両方できるものを、片方だけにする理由がない。**

Issue #6 で実際に測ったところ、**当時の zumen は落ちていた。**

| | ライト | ダーク |
|---|---|---|
| `primary` vs `muted`（地） | **1.01:1** | **1.02:1** |
| `primary` vs `muted`（枠） | **1.95:1** | **1.50:1** |

意味の語を 2 つ持っているのに、**白黒にすると見分けられなかった。**

### なぜ濃淡ではなく破線か

最初は文字の濃さで離そうとした。**姉妹アプリの淡い色はどれも白の近くに集まっている**ので、
地では 1.01:1 までしか離れない。文字を落として離そうとすると、
**今度はその文字が自分の地から読めなくなった**（2.42:1）。

**破線は色を捨てても残る。** §2.3 の「人が置いた印は**太さで示し、色で示さない**」と同じ考え。
囲みの破線（`6 4`）とは別の間隔（`3 3`）にして、囲みと取り違えないようにしてある。

`vivid` で塗り潰したときの文字は、**アクセントの明るさから機械が選ぶ**。
姉妹アプリのアクセントはライトで暗くダークで明るいので、
どちらの文字が読めるかを人が覚えていると、**あちらが色を変えたときに崩れる。**

### 検査

`test/legibility.test.ts` が、**ライト / ダーク × safe / vivid の 4 通りすべて**で見る。

- 箱の文字 vs 地 … 4.5:1 以上
- 線 vs 地 … 3:1 以上
- 体裁の語どうし … 3:1 以上、**または破線が違う**

**下回ったら `pnpm test` が落ちる。** 見た目の選択を増やすときも、この 4 通りが増えるだけ。

### まだ決めていないこと

Issue #6 が挙げた「採るべきでない」のうち、**等角投影（3D）と手描き風は実装していない。**
やるなら `intent` の 3 つ目になるが、**いまは足さない。**
選択肢の維持費が本体価値を食う（D11）。**実際に要る場面が出てから足す。**

---

## 8. 図の色は、姉妹アプリより濃い（**人が決めた**。2026-09-11）

§1 は「色の値を zumen が独自に決めない（姉妹アプリに揃える）」と書いています。
**図の中だけは、当てる先を変えます。**

### なぜ

> 作れる図がやはりしょぼいですね。

その指摘を受けて測ったところ、**箱も囲みも地から見えていませんでした。**

| | 比 | |
|---|---|---|
| 箱の枠 vs 地 | **1.47:1** | 非文字の下限は 3:1 |
| 囲みの枠 vs 地 | **1.18:1** | |
| 囲みの地 vs 地 | **1.03:1** | 事実上、囲みが無い |

**姉妹アプリの淡さは間違っていません。** あちらは静かな文書 UI で、
罫線がヘアラインなのが正しい。**図は一目で読ませるもの**で、要件が違います。

### 変えたこと

| | 前 | 後 |
|---|---|---|
| 箱の枠 | `--border-strong` | **地から最も遠いインク**（ライトは `--text-primary`、ダークは白） |
| 囲みの地 | `--bg-subtle` | `--neutral-bg` |
| 囲みの枠 | `--border` | `--text-secondary`（破線のまま） |

**新しい色は作っていません。** 姉妹アプリのトークンから、**当てる先を変えただけ**です。

「地から最も遠いインク」を機械に選ばせているのは、
**ダークでは `--text-secondary` がアクセントと 1.3:1 まで近づく**ためです。
人が覚えていると、姉妹側が色を変えたときに崩れます。

### 検査

`test/legibility.test.ts` が、**箱と囲みが地から見えること**も見ます。
以前は「体裁の語どうしの区別」しか見ておらず、**1.47:1 を通していました。**

---

## 9. 図の向きは、正本が決める（既定は横）

**ずっと縦（`DOWN`）で固定**でした。その結果どの図も縦長になり、
**構成図ではなく流れ図に見えていました**（縦横比 1:1.8）。

```yaml
direction: right   # 省略すると right
```

**既定を横にしました。** 構成図は横に読むもので、画面も資料も横長です。
`down` は手順や流れを表すときに選びます。

`rank` や `order`（[#2](https://github.com/meta-taro/zumen/issues/2)）は入れていません。
**並びは人の承認の領分**に踏み込みます。向きは 2 つに 1 つで、
**図全体の読み方**にしか効かないので、別扱いにしました。
