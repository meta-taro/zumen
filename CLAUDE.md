# zumen

> このリポジトリは、人と AI エージェント（Claude Code）が一緒に開発することを前提に構成されています。
> AI エージェントは以下を必ず守ってください。

## 必読

- **`.claude/rules/product-baseline.md`** — 開発のベースルール。**最優先で従うこと**。
- **`PRD.md`** — このプロダクトの方向性・スコープ・やらないこと。
- **`.claude/roadmap.md`** — フェーズと進め方。
- **`.claude/decisions.md`** — 決定と、その理由。**未決は D2 / D3**。
- **`.claude/issues/`** — 着手すべきローカル Issue。**001 が最初の一手**。
- **`docs/origin/`** — 受領した原案の原文。PRD は原案の要約ではなく、
  **原案から何を動かしたかを書いた文書**。動かしていない部分は原案を読むこと。

## この製品に固有の禁止事項（最優先）

1. **「AI が図を出す」ところで満足しない。** 勝負どころは
   **人が手で直した分が、次の生成で壊れないこと**の 1 点。
   生成品質の改善に時間を使う前に、必ず手直し保持を測る（Issue 001）。
2. **保存形式を決める前に GUI を作り込まない（D2）。**
   独自形式のまま進めると、この製品が終わった時点で図が読めなくなる。
   **テキスト正本・仕様が実装から分離**が条件。
3. **既存の作図ソフトの機能一覧を目標にしない。**
   選択・ドラッグ・リサイズ・接続・Undo は、**AI の書き換えを人が承認できる**のに
   要る分だけ実装する。編集機能そのものが目的ではない。
4. **人が直す往復を消す方向へ進めない。**
   図は「理解を共有するため」に描く。全自動で出るだけの図は、
   誰も理解しないまま貼られる。**往復を残すこと自体が目的**（UX チェックリスト §14）。
5. **Web 版・共同編集・他形式インポートを先に作らない**（PRD §4）。

## 守ることの要点（詳細は product-baseline.md）

- Node 側は **pnpm のみ使用**（npm / yarn 禁止）。
- 実装前に計画を立てる。小さいフェーズで作業。**テストを後回しにしない／落ちるテストを消さない。**
- **commit は AI、push は人間。**人間の確認なしに push しない。
- 秘密情報（鍵・トークン）は **AI が作らない・置かない・貼らない。**
- 進捗は `.claude/project-status.md` に随時記録。**テストが無い状態で「完了」と書かない。**
- **public リポジトリです。**コード・文書・commit history に個人名・個人メールアドレスを
  残さないこと（`.github/workflows/oss-privacy-check.yml` が検出します。
  ベースルール §32）。初回設定は下記。

### 初回設定（clone した直後に 1 回）

```bash
git config user.email "<GitHub の noreply メール>"
git config user.name  "<GitHub アカウント名>"
```

**この設定を飛ばすと、1 個目の commit から個人メールが history に焼き付きます。**
history に入ったものは書き換えても完全には消えません（fork / clone 済みのコピーに残る）。

検査はローカルでも同じものが回ります。

```bash
bash .github/scripts/oss-privacy-check.sh
```

## いまやること — Phase 0

**実装はまだありません。着手順は `.claude/roadmap.md`。**

| 次の一手 | 誰が |
|---|---|
| **Issue 001**（手直しが再生成で壊れないかの実測）| AI |
| **Issue 002**（保存形式 D2 の決定）| AI が案を出し、人が決める |
| **Issue 006**（図形とアイコンのライセンス確認）| AI |
| リポジトリ secret `OSS_DENY_WORDS` の設定 | 人 |

**Issue 001 の結果が No なら、この企画は畳みます。**
先に GUI を作り込むと、畳む判断ができなくなります。

## 進捗管理

- `.claude/project-status.md` … 現在フェーズ・完了/未完了・次タスク・既知問題
- `.claude/decisions.md` … 技術的決定と、その理由
- `DESIGN.md` … **人が書く**設計判断（AI が代筆しない）

## 日課（AI エージェント向け）

### セッション開始時

1. `git pull --ff-only`
2. `gh issue list --state open` ＋ `gh issue list --state closed --limit 10`
   （**close 済 Issue にも後追いで指示や訂正が入ることがあるため、必ず両方見る**）
3. open Issue は全件 `gh issue view <番号> --json title,body,comments,author,createdAt --jq '.'` で本文＋コメントを確認
   （`--comments` は出力が空のまま exit 0 することがあり、「読んだが何も無かった」と区別できないため使わない）
4. 何を確認し、どれから着手するかを返す

### Issue への反応（着手前）

- 新規 Issue・新規コメントには、**着手前に最低 1 回反応する**（「読みました。〇〇から着手します。」）
- **沈黙は「読んでいない」「止まっている」「無視した」と区別がつきません。**
- 「承知しました」だけを返さない。**できていないなら、できていないと認め、対策と日付を出す。**
  前提がおかしいと思うなら異議・代案を出す。

### セッション終了時

1. `.claude/project-status.md` に進捗を記録（テストが無い状態で「完了」と書かない）
2. 完了した Issue は `gh issue close <番号> --comment "..."`

### git pull の 3 タイミング

1. **セッション開始時**: `git pull --ff-only`
2. **commit する直前**: `git pull --rebase --ff-only`
3. **人間が push するとき**: ff エラーなら `pull --rebase` してから再 push
